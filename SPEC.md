# MantaPhotos - 产品需求规格文档

> **文档版本:** v0.0.16
> **创建日期:** 2026-05-25  
> **最后更新:** 2026-06-06（v0.0.16）
> **作者:** Venk
> **状态:** 待评审  
> **交互原型:** [`demo/index.html`](demo/index.html)（HTML/CSS/JS，用于产品交互验证）  
> **技术规格:** [TECH.md](TECH.md)（架构、数据库、实现方案与开发计划）

---

## 变更日志

### 里程碑原则

> 每当版本号尾数达到 **10 的倍数**（如 v0.0.10、v0.0.20、v0.1.0…），即为一个**小里程碑**。  
> 小里程碑版本说明须包含：本阶段新增功能摘要、重点完成的交互设计、已修复的问题，以及对后续迭代的方向指引。

---

### v0.0.16（2026-06-06）— 时间线 UI 精细化：卡片重设计、搜索栏、年份索引

**阶段：时间线模块达到生产级标准，照片预览卡片、搜索与年份导航体验全面提升**

#### 变更内容

**卡片设计重构**
- 彻底重新设计卡片布局：顶部展示类型 badge（事件/回忆）＋旅行 badge＋月份标签，中部大标题（带彩色图标），底部水平滚动照片预览条（70px 高，支持横向滑动，隐藏滚动条）
- 去除重复的照片张数信息：仅在照片条末尾展示"+N张"溢出芯片，移除标题行中的`· N 张`文字，视觉更干净
- 卡片左侧添加 3px 色条：事件=紫色，回忆=琥珀色，一眼区分类型
- 年份索引位移至右侧 16px，芯片更宽松（padding 增大），active 状态自动跟随滚动高亮

**年份标题间距修复**
- 使用 `top: -20px; padding-top: 28px; margin: 0 -20px` 技巧：完全消除 `.main-content` 的 `padding: 20px` 导致的 sticky 年份标题与顶栏之间 20px 间隙，年份标题滚动贴顶时与导航栏无缝衔接
- 年份标题背景延伸至页面全宽（负 margin 技巧），磨砂玻璃效果覆盖完整

**时间线内容区布局**
- `#timelineContainer` 增加 `padding-right: 52px`，为右侧年份快速跳转索引预留空间，卡片内容不再与年份索引重叠

**时间线搜索栏**
- 增加 ESC 键支持：搜索框有内容时按 Escape 清除搜索词并失焦
- 搜索空状态有友好提示页（图标＋文字）

**年份快速跳转优化**
- 使用 `IntersectionObserver` 实时追踪当前可见年份，自动同步索引高亮状态，无需手动操作
- 点击跳转计算精确滚动偏移（考虑 sticky 标题高度），滚动至目标年份准确定位

#### 后续迭代方向（v0.0.17+）
- 时间线地图视图
- 事件封面图选取与编辑
- 时间线与照片页双向关联导航

---

### v0.0.15（2026-06-05）— 时间线全面升级 & 废片篓交互完整修复

**阶段：时间线从年份选择器升级为连续滚动式事件墙；废片篓移入/移出/清空交互闭环**

#### 变更内容

**废片篓交互修复**
- 详情页废片篓按钮：移入废片篓后按钮变为金黄色（`in-trash` CSS 类），移出后恢复默认样式，明确体现当前状态
- `restoreAllTrash()`：还原全部照片后自动清除多选选中状态与工具栏，避免遗留"已选 N 张"浮条；若当前处于废片篓筛选视图则自动退出，切回全部照片
- `confirmDelete()` 清空废片篓后：自动退出废片篓筛选视图，重置活跃快捷筛选与清空按钮状态
- `moveCurrentToTrash()`：从废片篓视图内移出照片时，自动跳转到下一张照片或关闭查看器，避免查看器展示已消失的条目

**时间线页全面升级**
- **连续滚动**：移除右上角年份选择器（`<select>`/左右切换按钮），时间线改为全年份纵向连续滚动；所有年份数据一次渲染，用户可持续下滑浏览历史
- **年份分组标题**：每年用年份大标题（28px 粗字）+ 横线分隔，标题吸顶（`position: sticky`）方便定位
- **事件 + 月份回忆双轨**：时间线条目分为两类
  - `event`（事件）：有明确主题的游玩/活动，带专属图标与紫色标签；2024/2025 各有 5-6 个预设事件
  - `memory`（月份回忆）：月度照片集锦，带橙色标签；2026 年由真实照片数据动态生成
  - 同月内事件排在回忆前，两类可并存
- **丰富 Mock 数据**：新增 2024~2025 共 14 个事件/回忆条目（三亚之旅、张家界登山、成都骑行、黄山云海徒步、杭州赏樱、毕业季合影、春节家宴等）；2026 年月份回忆由照片池动态生成
- **点击进入事件页**：点击任意时间线条目进入专属事件详情页，展示该事件全部关联照片（网格视图，支持缩放）
- **查看器完整适配**：事件页照片支持点击打开全屏查看器（`←/→` 在事件集内导航），支持详情面板、收藏、废片篓、删除等全部查看器功能；关闭查看器后清除临时照片集，回到事件页上下文
- **条目卡片设计**：时间线条目改为圆角玻璃卡片，包含类型标签、日期、标题（带图标）、描述文字、照片缩略图（最多4张 + 更多数量气泡）、照片张数统计；hover 时卡片高亮并缩略图微放大
- **多网格支持**：`applyGridSize()` 同时更新照片页网格和事件页网格的 CSS 变量与圆角；缩放动画跟随当前活跃页面的网格

#### 后续迭代方向（v0.0.16+）
- 时间线地图视图（按地点聚合事件/照片）
- 事件页支持编辑事件标题、描述、关联照片
- 时间线照片与正式照片库双向关联（跨页查看同一张照片）

---

### v0.0.14（2026-06-05）— 全屏照片查看器重设计 & 侧边栏/搜索一致性优化

**阶段：照片查看体验从"弹窗信息展示"升级为"沉浸式全屏预览 + 按需信息展开"**

本版本完整重设计照片查看弹窗：默认全屏展示照片/视频，顶部信息最小化；通过液态玻璃样式"详情"按钮（快捷键 `i`）按需展开右侧详情面板，包含文件名/时间/位置/人物/评分/维度/标签/描述/建议等完整信息。同时修复侧边栏与搜索组件的一致性问题，并补全若干遗漏筛选项。

#### 新增 / 重构功能

| 功能 | 说明 |
|------|------|
| 全屏照片查看器 | 点击照片进入全屏查看模式（`z-index: 250`），照片/视频充分利用屏幕尺寸；深色半透明背景（`rgba(0,0,0,0.94)`） |
| 液态玻璃详情按钮 | 查看器底部右侧液态玻璃样式按钮（`backdrop-filter: blur(24px) saturate(200%)`），提示快捷键 `i`；激活时呈现紫色玻璃态 |
| 右侧滑入详情面板 | 点击"详情"后从右侧滑入 360px 详情面板（`transform: translateX`），图片区域自动左移适配；面板采用暗色/白色毛玻璃背景，支持深/浅色主题 |
| 详情面板内容 | 文件名（HEIC/JPG/ARW/MP4 等真实格式）作为主标题；时间、地点、媒体类型、设备、涉及人物作为元数据行；综合/美学评分 pill（颜色跟随分数档位配色）；6 维度进度条；标签云；内容描述+照片回忆+保留建议+相似组信息 |
| 左右导航 | 查看器内方向键（`←` / `→`）及浮层按钮可在当前过滤结果集中切换照片，边界自动禁用导航按钮 |
| 键盘快捷键 | `Esc` 关闭查看器；`i` 切换详情面板；`←` / `→` 切换照片；查看器打开期间其他快捷键不响应 |
| Mock 文件名 | 根据媒体类型和设备类型自动生成模拟文件名（如 `IMG_0042.HEIC`、`DSC_0042.ARW`、`VID_20260601_0042.MP4`、`Screenshot_20260601_0042.PNG`） |
| 状态筛选修复 | 搜索浮层 `statusDefs` 中 `favorite` key 修正为 `isFavorite`（修复照片计数始终为 0 的 bug）；补充「废片篓」(`isInTrash`) 状态项，与侧边栏对齐 |
| 搜索状态新增废片篓 | 搜索浮层"状态"分区新增「废片篓」筛选项，对应 `isInTrash` 字段；`computeFindFilter` 同步过滤逻辑 |

#### 交互细节

- 查看器打开时默认隐藏详情面板，用户主动按 `i` 或点按钮才展开——"照片优先，信息按需"原则
- 详情面板关闭时底部导航 `viewer-next` 按钮位于画面右侧 16px；面板打开时右移 376px，避免与面板重叠
- 底部 gradient 遮罩使文件名与详情按钮在深色背景下始终可读
- 切换照片时若面板已打开，自动刷新面板内容为新照片信息
- 收藏操作立即同步更新面板内收藏按钮状态（心形红色/普通）

---

### v0.0.13（2026-06-05）— 侧边栏快捷筛选体系重构与 AI 进度组件交互升级

**阶段：侧边栏从"导航入口"升级为"实时快捷筛选面板"**

本版本将侧边栏定位为搜索组件的精简版，构建完整的快捷筛选体系；同时重构 AI 分析进度为顶栏精简版+悬浮完整版，并对搜索浮层各分区补全清空操作与标题对齐。

#### 新增 / 重构功能

| 功能 | 说明 |
|------|------|
| 侧边栏大类重排 | 顺序调整为：**类型 → 评分 → 时间 → 状态 → 设备与来源 → 人物 → 标签** |
| 新增「类型」快捷筛选 | 四项：照片 / 视频 / Live / 截图；单选，支持清空；点选即实时过滤照片墙 |
| 评分区内置快捷按钮 | 「高分照片」「废片篓」快捷按钮嵌入评分滑块下方，点击时自动设置操作符（`>` / `<`）与对应档位分值，与分数区间配色设置联动 |
| 状态筛选收窄为单选 | 仅保留「我的收藏」「重复/相似」两项，高分/废片已归入评分区；再次点击同一项即清空，支持"清空"按钮 |
| 时间快捷筛选 | 单选；默认展示 近3天/近一周/本月，「更多」展开全部 6 项（今天/近3天/近一周/本月/近一月/本年）；支持清空 |
| 设备与来源快捷筛选 | 单选；默认展示前 3 项，「更多」展开全部 6 项；支持清空 |
| 人物快捷筛选 | 多选；内置搜索框，输入关键词实时过滤所有人物；默认展示前 3，「更多」前 6；支持清空 |
| 标签快捷筛选 | 单选；移除「全部」选项（无选中 = 显示全部）；支持清空 |
| 快捷筛选数量角标隐藏 | 侧边栏快捷筛选区不再显示照片数量，保持视觉简洁 |
| AI 进度迁移到顶栏 | 从侧边栏移至顶栏主题按钮左侧，常驻**精简版**（状态图标+进度条+百分比+暂停/停止），鼠标悬浮向下弹出**完整版**（暗色毛玻璃弹层，内含标题/状态/进度详情/控制按钮） |
| AI 弹出层视觉加强 | 弹出层背景由半透明浅色改为 `rgba(14,14,22,0.82)` 暗色底 + `blur(48px) saturate(200%)` 磨砂，确保在浅色背景下内容清晰可读 |
| AI 精简版图标颜色修复 | 修复状态切换时 `ai-mini-state` 样式类被覆盖导致图标颜色失效的问题，运行中保持绿色 |
| 搜索浮层标题对齐 | 所有分区标题（媒体类型/评分/时间范围/状态/设备与来源/地点/人物/标签）内容统一包裹 `<span>`，标题 icon 与文字始终靠左对齐，清空按钮靠右 |
| 搜索浮层各分区清空按钮 | 所有筛选分区右上角补全「清空」按钮，有活跃条件时显示，一键重置该分区 |
| AI 分析进度刷新提速 | 刷新间隔由 10 s 改为 5 s，demo 每次批量 100–150 张 |

#### 重点交互设计决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 高分/废片归入评分区 | 作为评分滑块的快捷入口，而非独立状态筛选项 | 与滑块一体，语义更清晰；避免"状态"与"分数"概念混淆 |
| 侧边栏隐藏数量角标 | 移除各快捷项的计数 badge | 计数实时变化会造成视觉抖动，且侧边栏只需快速定位，精确数量由照片墙展示 |
| 类型筛选单选 | 仅允许同时选一种媒体类型 | 多选时过滤语义复杂；单选更直观，常见场景均为"只看照片"或"只看视频" |
| AI 进度弹出层暗底 | 固定暗色背景而非跟随主题 | 进度卡内含彩色进度条与状态图标，暗底对比度最优，浅色主题下也不会"洗白" |

#### 后续迭代方向（v0.0.14+）

- 侧边栏快捷筛选与查找浮层联动（点侧边栏条件可同步反映在查找浮层的状态）
- 角标配色方案预设与本地持久化
- 侧边栏"任务"区块（已有 AI 分析入口，补充清理任务/批量操作入口）

---

### v0.0.12（2026-06-04）— 照片页角标液态玻璃化、分数区间可配置与缩放细节打磨

**阶段：照片页角标与缩放交互的精细化**

本版本聚焦照片页的视觉与微交互打磨：评分角标改为可配置的四档马卡龙液态玻璃样式，缩放按钮去卡顿，并修复布局抖动；同时补齐站点 favicon。

#### 新增 / 重构功能

| 功能 | 说明 |
|------|------|
| 角标液态玻璃化 | 评分角标由纯色块改为半透明马卡龙色层叠加暗底 + `blur(18px) saturate(170%)` 背景模糊 + 高光边框 + 文字阴影，通透且文字清晰 |
| 四档配色（蓝>绿>黄>粉） | 默认 蓝 100–80 / 绿 80–60 / 黄 60–40 / 粉 40–0；低饱和马卡龙色系，区间互斥并覆盖 0–100 |
| 分数区间可配置 | 设置 →「分数标签 → 分数区间配色」：实时配色预览条 + **三个可拖拽滑块手柄**控制 3 个分界（`t3≤t2≤t1` 自动钳制），拖动即时刷新照片墙；含「恢复默认（80/60/40）」 |
| 角标显隐交互 | 照片页 美学/综合 分段控件：点另一项切换维度；**再点当前项即隐藏角标**。隐藏态下当前维度按钮保留弱化暗色高亮（仍明显有色），另一按钮维持默认外观 |
| 缩放到最后两级自动隐藏角标 | 16 / 32 列缩略图过小，自动隐藏角标避免遮挡 |
| 站点图标 | 新增 favicon（ico/16/32/apple-touch-icon/webmanifest），Chrome 标签页优先使用 32×32 PNG |
| 显示底部功能栏开关 | 设置中新增开关（默认开启），可隐藏底部导航以获得更大内容区 |

#### 已修复的问题

| 问题 | 说明 |
|------|------|
| 缩放按钮卡顿感 | 移除照片卡 `width` 过渡（Flexbox 下逐帧触发全量 reflow），列宽即时切换；网格改用 `transform: scale(0.995→1) 0.1s`（锚定左上角，走 compositor 线程）作为轻量反馈，无抖动 |
| 切换 3↔4 档布局横向抖动 | `.main-content` 由 `overflow-y:auto` 改为 `overflow-y:scroll + scrollbar-gutter:stable`，始终预留滚动条槽位，列数变化不再使工具栏平移 |

#### 重点交互设计决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 分数区间配置 | 由 3 个分界值派生四档区间 | 天然保证互斥且覆盖 0–100，杜绝重叠/留空的非法状态 |
| 配置控件形态 | 色条上叠加 3 个拖拽滑块（去掉数字输入框） | 所见即所得，拖动即预览，最直观 |
| 角标隐藏入口 | 复用分段控件“再点一次”，而非独立设置开关 | 入口就在控件旁，减少进设置的路径 |
| 缩放反馈 | 用 transform 而非 width 动画 | width 动画触发 layout，transform 走合成层，彻底消除卡顿 |

#### 后续迭代方向（v0.0.13+）

- 角标配色方案预设（如色弱友好配色）与持久化存储
- 缩放级别、角标显隐等偏好的本地持久化

---

### v0.0.11（2026-06-04）— 搜索与筛选统一为全局「查找」浮层

**阶段：以乔布斯式产品审视重构搜索/筛选体验**

本版本将原先分离的「搜索页」与「筛选页」合并为一个全局、可在任意页面唤起的「查找」浮层（Spotlight 风格），并让筛选结果实时作用于照片墙。

#### 新增 / 重构功能

| 功能 | 说明 |
|------|------|
| 全局「查找」浮层 | 合并搜索与筛选；`⌘K` / 顶栏居中搜索胶囊 / 底部「查找」图标唤起；Spotlight 风格居中磨砂面板；点击空白或 `Esc` 关闭 |
| 导航收敛为 4 项 | 顶部与底部导航仅保留 照片 / 时间线 / 报告 / 设置；移除独立的「搜索」「筛选」Tab 与页面 |
| 顶部居中搜索胶囊 | 顶栏中央放置「搜索照片…」胶囊（左/中/右三段均衡布局，桌面宽度精确居中），宽度 330px |
| 照片墙实时更新 | 浮层内任何条件变化即时刷新身后的照片墙；取消「预览/在照片墙中查看」按钮与中间态 |
| 浮层只显计数 | 浮层顶部显示「匹配 N 张 · 照片页已实时更新」，不再在浮层内重复渲染照片预览 |
| 新增筛选维度 | 媒体类型（照片/视频/Live/截图/RAW）、收藏、人物、地点芯片、设备来源 |
| 设备来源筛选 | 状态与来源区新增一行：Pocket、运动相机、无人机、单反相机（多选，含命中计数与图标） |
| 评分实时筛选 | 单条评分条件：维度单选标签（美学/综合/技术/内容/情感/稀有/独特）+ 大于/小于 + 分数条（滑块+数字双向同步、渐变视觉反馈）；改动即时生效，默认「美学 > 0」即不过滤 |
| 区块顺序 | 浮层筛选顺序：媒体类型 → 评分 → 时间范围 → 状态与来源 → 地点 → 人物 → 标签 |
| 主题三态 | 主题按钮循环「跟随系统 → 浅色 → 深色」，进入主页默认跟随系统，并实时监听系统深浅色变化 |
| 浮层可读性增强 | 面板背景模糊 +20%（30→36px）、背景色加深约 5%，遮挡层加深，确保繁忙照片墙之上内容清晰 |
| 删除与查找解耦 | 「查找」不再承载删除动作；删除归口到多选工具栏与照片详情页 |

#### 重点交互设计决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 搜索与筛选关系 | 合并为单一「查找」浮层 | 自然语言与属性芯片 AND 叠加，用户无需在两个页面间切换 |
| 浮层 vs 独立页 | 全局浮层（保留当前上下文） | 查找是“入口能力”，应随处可唤起且不打断浏览 |
| 结果呈现 | 照片墙实时更新 + 浮层仅计数 | 去掉“预览/应用”中间态，所见即所得 |
| 评分条件 | 单条实时条件 + 分数条 | 保留原分数条视觉与即时反馈，默认 美学>0 不过滤 |
| 主题 | 默认跟随系统 | 符合 macOS 习惯，进入即合适 |
| 删除入口 | 移出查找 | 降低查找场景的误删焦虑，职责单一 |

#### 后续迭代方向（v0.0.12+）

- 查找历史与常用查找保存
- 人物/地点的 AI 聚合分组与缩略图
- 多选删除三步确认（含视频统计）

---

### 🎯 v0.0.10 小里程碑（2026-05-28）

**阶段：交互原型 Demo 完整版 · v0.0.1 → v0.0.10**

本里程碑完成了从规格草稿到完整可交互原型的首个完整迭代周期。

#### 新增功能

| 功能 | 版本 | 说明 |
|------|------|------|
| 照片详情弹窗 | v0.0.6 | 双评分 + 6维度分 + 相似组 + 保留建议 + 内容描述 + 照片回忆 |
| 自然语言搜索 | v0.0.3 | 分数/时间/标签/地点语义解析，热门词推荐，结果定位到照片页 |
| 时间范围筛选 | v0.0.3 | 天/周/月/年/特殊（旅行/节假日）分排芯片，单选 + 已选展示条 |
| 7维评分筛选 | v0.0.3 | 大于/小于 + 滑块/数字框双向同步，筛选预览 + 移至废纸篓 |
| 时间线页 | v0.0.4 | 按年月事件轴，年份切换，旅行角标，空状态 |
| AI 提示词配置弹窗 | v0.0.5 | 7 任务展开编辑，美学评分锁定不可编辑 |
| 评分权重配置弹窗 | v0.0.5 | 6维滑块，总和校验，恢复默认 |
| 照片角标分段控件 | v0.0.8 | 从设置页移至照片页工具栏，两选项同时可见（Segmented Control） |
| 工具栏缩放控件 | v0.0.9 | `−`/`+` 按钮，7级列数（2/4/6/8/12/16/32），照片 width 丝滑过渡 |
| 排序联动 | v0.0.9 | 4选项，按分排序维度跟随分段控件（美学/综合）动态更新 |
| 侧边栏边缘标签 | v0.0.9 | 贴合侧边栏右边缘的胶囊折叠标签，平滑滑动 |
| 侧边栏 overlay 模式 | v0.0.10 | 侧边栏叠加在照片上方，磨砂玻璃背景，不推开主内容区 |
| 滚动自动隐藏侧边栏 | v0.0.10 | 累计滚动 >10px 自动隐藏，停滚后恢复至滚动前状态 |
| 标签云默认 8 个 | v0.0.10 | 全部/风景/人物/建筑/美食/旅行/宠物/夜景，更多按钮带图标+样式 |

#### 重点交互设计决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 角标切换方式 | Segmented Control | 两选项同时可见，零认知负担，不需要进设置页 |
| 缩放按钮语义 | `−` = 缩小（更多列）, `+` = 放大（更少列） | 与系统惯例一致 |
| 侧边栏遮挡方式 | overlay，可挡住照片 | 不压缩内容区宽度，磨砂玻璃背景产生层次感 |
| 滚动隐藏策略 | 累积 >10px 隐藏，停滚恢复 | 避免误触，滚动结束后还原用户意图 |
| 更多标签按钮 | 图标+文字小胶囊 | 纯文字按钮缺乏视觉引导，图标让可点击性更明确 |

#### 已修复问题

| 问题 | 修复版本 | 说明 |
|------|----------|------|
| `−`/`+` 按钮 delta 反转 | v0.0.10 | 原代码 `−` 调用 `adjustGridSize(-1)` 导致放大，已对调 |
| 缩放按钮禁用条件反转 | v0.0.10 | `outBtn.disabled` 与 `inBtn.disabled` 边界条件互换修正 |
| 排序"按分"仅支持综合 | v0.0.9 | 改为跟随分段控件维度，支持美学/综合双模式 |
| 侧边栏折叠按钮位置不直觉 | v0.0.9 | 从顶部栏移至侧边栏右边缘，控件紧贴所控对象 |

#### 后续迭代方向（v0.0.11+）

- 照片页多选 → 批量删除走三步确认流程（含视频统计）
- 自定义分析任务编辑器
- 报告 Markdown 内嵌阅读器 + 导出桌面
- iCloud 一键下载区块

---

### 历史版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.0.1 | 2026-05-25 | 初版需求规格基线：产品目标、AI分析任务分类、双评分体系、核心数据结构确立。 |
| v0.0.2 | 2026-05-26 | 核心页面与交互规格成稿：照片网格、时间线、筛选、报告、设置页主流程。 |
| v0.0.3 | 2026-05-26 | 搜索与筛选规格成稿：自然语言搜索解析规则、7维评分筛选、时间范围分排芯片。 |
| v0.0.4 | 2026-05-27 | 时间线与报告规格成稿：年月事件轴、旅行标记、报告列表与 Markdown 查看方案。 |
| v0.0.5 | 2026-05-27 | 设置与自定义任务规格成稿：模型管理、权重配置弹窗、提示词弹窗、锁定任务规则。 |
| v0.0.6 | 2026-05-27 | 照片详情弹窗与双评分规格成稿：维度分条形图、相似组、保留建议、内容描述、照片回忆。 |
| v0.0.7 | 2026-05-28 | Demo 与规格对齐（评审版）：第八章交互规范从 Demo 反向整理，补全 Demo 能力摘要。 |
| v0.0.8 | 2026-05-28 | 照片角标分数切换从设置页迁移至照片页工具栏分段控件，减少用户进设置的操作路径。 |
| v0.0.9 | 2026-05-28 | 工具栏控件重排（缩放→分段→排序），7级网格缩放丝滑过渡，4选项联动排序，侧边栏边缘折叠标签。 |
| **v0.0.10** | **2026-05-28** | **🎯 小里程碑**：照片页交互首个完整迭代。修正 `−`/`+` 缩放语义与禁用逻辑；侧边栏重构为 overlay 模式（叠加在照片上，磨砂玻璃背景），tab 移至左侧与面板动画同步；滚动 >10px 自动隐藏、停滚后 0.5s 延迟恢复；悬浮触发（0.8s 可配置，0~5s 支持关闭）；标签云默认展示 8 个标签（增加宠物/夜景），更多按钮升级为图标胶囊样式。首个可独立演示的照片页交互闭环，v0.0.11 起将打磨其余页面。 |
| v0.0.11 | 2026-06-04 | 搜索与筛选合并为全局「查找」浮层（`⌘K`/顶栏居中搜索胶囊/底部查找入口唤起，Spotlight 风格）；导航收敛为 照片/时间线/报告/设置 4 项，移除独立搜索/筛选页；筛选条件实时作用于照片墙、浮层仅显计数；新增媒体类型/收藏/人物/地点/设备来源（Pocket·运动相机·无人机·单反相机）筛选；评分改为单条实时条件（维度单选标签+分数条，默认 美学>0 不过滤）；主题新增「跟随系统」并默认；浮层虚化加强、背景加深以提升可读性；删除从查找剥离。 |
| v0.0.12 | 2026-06-04 | 照片页评分角标改为四档马卡龙液态玻璃（蓝 100-80/绿 80-60/黄 60-40/粉 40-0）；分数区间可在设置中通过拖拽滑块配置（3 分界派生、互斥覆盖 0-100、实时预览）；分段控件「再点当前项」隐藏角标、隐藏态保留当前维度弱高亮；缩放到最后两级自动隐藏角标；移除照片卡 width 过渡、改用 transform scale 反馈消除缩放卡顿；`scrollbar-gutter:stable` 修复切档布局横向抖动；新增站点 favicon 与底部功能栏显隐开关。 |
| v0.0.13 | 2026-06-05 | 侧边栏快捷筛选体系重构（类型/评分/时间/状态/设备/人物/标签 7大类）；AI分析进度移至顶栏精简版+悬浮完整版；搜索浮层分区补全清空与标题对齐。 |
| v0.0.14 | 2026-06-05 | 照片查看从弹窗升级为全屏查看器：液态玻璃详情按钮（i 快捷键）展开右侧面板；文件名/时间/位置/人物/评分/维度等完整元数据；视频/Live 播放控制；废片篓移入/移出/清空/恢复；键盘快捷键（T/F/⌘D/Space）。 |
| v0.0.15 | 2026-06-05 | 时间线连续滚动+事件/回忆双轨+点击进入事件页+查看器完整适配；废片篓移入/移出/清空交互全面修复。 |
| **v0.0.16** | **2026-06-06** | **时间线卡片重设计（照片预览条、左侧色条、去除重复张数）；sticky 年份标题间距修复；右侧年份快速跳转索引（IntersectionObserver 自动高亮）；内容区右边距为索引预留空间；搜索栏 ESC 清除支持。** |

---

## 一、产品概述

### 1.1 产品定位

**MantaPhotos** 是一款运行在 macOS Tahoe 26.5+ 上的本地 AI 照片管理应用。接入 Qwen 开源多模态大模型，对用户照片库（照片+视频）进行深度内容分析、打分、打标签，帮助用户快速删除废片、快速搜索珍贵记忆。

### 1.2 核心价值

| 痛点 | 解决方案 |
|------|----------|
| 照片太多，找不到想要的那张 | AI 自然语言搜索 |
| 废片占用空间，删除太慢 | 一键按分数筛选删除 |
| 重复照片占据空间 | AI 识别相似/重复照片 |
| 照片没有分类标签 | AI 自动打标签 |
| 历史照片太多，首次导入慢 | 分年分批，首次先跑当年 |

### 1.3 目标用户

- 有大量照片库（10万+ 照片/视频）的 Mac 用户
- 希望高效管理照片、释放存储空间的用户
- 对 AI 辅助照片管理有需求的用户

### 1.4 iCloud 照片库策略

**核心原则：优先使用本地库照片**

| 场景 | 处理策略 |
|------|----------|
| **本地照片库** | 直接分析，无缝使用 |
| **iCloud 照片库（已下载原件）** | 视为本地照片，直接分析 |
| **iCloud 照片库（仅保留原件）** | 应用启动时提示用户"需要下载原件后才能分析"，提供一键下载入口 |
| **iCloud 照片库（下载中）** | 监听下载完成事件，完成后自动加入分析队列 |

> **说明：** 应用不管理 iCloud 同步逻辑，仅读取 Photos Framework 中的资源状态。若照片为 iCloud 远程资源（`isLocallyAvailable == false`），应用不会自动触发下载，但会提示用户。

---

## 二、功能需求

### 2.1 照片库接入

| 功能 | 描述 |
|------|------|
| **系统照片库授权** | 通过 Photos Framework 访问用户照片库 |
| **照片资源读取** | 读取照片元数据（EXIF、时间、地点、尺寸等） |
| **视频资源读取** | 读取视频元数据（时长、分辨率、编码格式等） |
| **实时监听** | 监听照片库变化，增量更新索引 |
| **iCloud 状态检测** | 检测资源是否本地可用，未下载时提示用户 |

### 2.2 AI 模型管理

#### 2.2.1 AI 模型策略

**Mac 应用采用两阶段模型策略：**

- **V1（初版）**：使用 macOS 系统内置的 **Apple Vision 框架**进行 AI 分析，无需用户下载任何模型。技术层面通过策略模式（`AIAnalysisProvider` 协议）预留扩展接口，供后续版本接入自定义模型。
- **后续版本**：在策略接口基础上，支持用户手动导入本地 Qwen VL 等开源多模态模型，可覆盖 Apple Vision 的分析结果（如美学评分），或执行 Apple Vision 不支持的深度分析任务（如照片内容描述、照片回忆）。

**后续版本支持的自定义模型列表（规划）：**

| 模型名称 | 参数量 | 类型 | 推荐场景 | 最低内存 |
|----------|--------|------|----------|----------|
| Qwen2.5-VL-3B | 3B | BF16/FP16 | 日常照片，轻量快速 | 8GB |
| Qwen2.5-VL-7B | 7B | BF16/FP16 | 高质量分析，较快 | 16GB |
| Qwen3-VL-4B | 4B | BF16/FP16 | ✅ **默认推荐**，新版模型 | 8GB |
| Qwen3-VL-8B | 8B | BF16/FP16 | 高质量分析 | 16GB |
| Qwen3-VL-30B-A3B | 30B (A3B MoE) | BF16/FP16 | 最高质量，A3B 专家路由 | 32GB (M3 Max) |

> **下载与存储（后续版本）：** 模型由用户手动触发下载，支持从本地导入 MLX 模型。格式、存储路径、HuggingFace 地址见 [TECH.md §1.3、§7.1](TECH.md)。

#### 2.2.2 模型下载管理

```
┌────────────────────────────────────────────────────────┐
│  设置 → AI 模型                                        │
├────────────────────────────────────────────────────────┤
│                                                        │
│  当前使用: Qwen3-VL-4B ✅ 已下载                       │
│                                                        │
│  可用模型:                                            │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Qwen2.5-VL-3B          [已下载 ▼]                 │ │
│  │ Qwen2.5-VL-7B          [下载 215MB]              │ │
│  │ Qwen3-VL-4B ⭐默认     [已下载 ▼]               │ │
│  │ Qwen3-VL-8B            [下载 430MB]              │ │
│  │ Qwen3-VL-30B-A3B       [下载 1.2GB]              │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  手动导入模型...                                       │
│                                                        │
│  下载位置: ~/Library/Application Support/             │
│            MantaPhotos/Models/                   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### 2.3 后台 AI 分析任务

#### 2.3.1 分析任务类型

**V1 实现任务：**

| 任务类型 | 描述 | Mac V1 实现方案 |
|----------|------|----------------|
| **美学评分** | 摄影美学维度评分（0-100）；截图内容强制 0 分 | Apple Vision `VNCalculateImageAestheticsScoresRequest`；截图由 `PHAsset.mediaSubtype` 标识，评分强制覆盖为 0 |

> **自定义模型覆盖（后续版本）：** V1 美学评分使用 Apple Vision。后续版本通过策略接口，支持用户导入 VLM 模型对美学评分进行覆盖，覆盖后分数来源标记为 `mlx_vlm`。

**后续版本规划任务：**

| 任务类型 | 描述 | 说明 |
|----------|------|------|
| **美学评分（自定义模型覆盖）** | 使用用户导入的 VLM 模型重新评分，覆盖 Apple Vision 结果 | 需用户导入模型后激活 |
| **综合评分** | 汇总多维度分数并输出综合分 0-100 | 依赖其他维度分完成后计算；美学维度分值必须来自美学评分任务输出 |
| **内容标签** | 场景、物品、动作等标签 | |
| **人物识别** | 检测人脸数量、人物数量 | |
| **地理位置分析** | 解析地点，标记旅行事件 | |
| **照片内容描述（≤300字）** | 描述拍摄主体与场景，并补充科普信息 | **独立任务**，与照片回忆分开维护；详见 2.3.2 |
| **照片回忆（≤200字）** | 生成当日叙事（日程、同行人、心情） | **独立任务**，与照片内容描述分开维护；详见 2.3.2 |
| **时间与事件分析** | 拍摄时间轴、旅行事件串联 | |
| **重复内容检测** | 检测相似/重复照片 | |
| **视频内容分析** | 视频关键帧抽取 + 内容分析 | |

**提示词策略（后续版本）：** 美学评分任务提示词锁定不可编辑；照片内容描述、照片回忆等 VLM 任务支持在设置中分别调整各自提示词（详见 2.3.2）。

#### 2.3.2 自定义提示词配置

每个分析任务提供**系统默认提示词**。  
其中「美学评分（严格）」为**系统锁定任务**：提示词不可编辑、不可替换、不可自定义。

**系统默认提示词（内置生成）：**

> **综合评分**
> ```
> 你是照片质量评估助手。请接收以下输入：
> 1) 美学评分任务输出的 aesthetic_score（严格任务结果）
> 2) 其他维度分数：technical/content/emotion/rarity/uniqueness
>
> 按当前权重计算综合评分 final_score（0-100），并给出一句总结。
>
> 请以JSON格式输出：
> {
>   "aesthetics_score": 85,      // 仅引用严格美学评分结果，不在本任务重算
>   "technical_score": 80,       // 清晰度等技术分
>   "content_score": 90,         // 内容价值分
>   "emotion_score": 75,         // 情感表达分
>   "rarity_score": 60,          // 稀有性分
>   "uniqueness_score": 70,      // 独特性分
>   "final_score": 80,           // 综合评分（加权平均）
>   "summary": "简要评价"        // 简短评价
> }
> ```

> **美学评分（严格，系统锁定）**
> ```
> 你是严格摄影美学评审员，仅依据摄影美学打分（构图、光线、色彩、主体层次）。
> 规则：若输入图片为截图/录屏/界面截取内容，aesthetic_score 必须为 0，不可例外。
>
> 请以JSON格式输出：
> {
>   "aesthetic_score": 0-100,
>   "is_screenshot": true/false,
>   "reason": "一句话原因"
> }
> ```

> **内容标签**
> ```
> 请分析这张照片的内容，输出标签。
> 标签分类：
> - 场景：海滩、山景、城市、乡村、夜景、室内、室外、森林、沙漠等
> - 物品：食物、动物、植物、汽车、建筑、天空、云朵、海洋、书籍等
> - 人物：人像、合影、自拍、背影、孩童、宠物、多人、单人等
> - 活动：旅行、聚餐、运动、婚礼、生日、会议、演出、购物等
> - 时间：日出、日落、正午、傍晚、夜晚、黄金时刻、蓝调时刻等
> - 天气：晴天、多云、雨天、雪天、雾天、雷电等
> - 情绪：开心、悲伤、浪漫、宁静、热闹、庄严、神秘等
>
> 请以JSON格式输出：
> {
>   "tags": [
>     {"category": "场景", "name": "海滩", "confidence": 0.95},
>     {"category": "活动", "name": "旅行", "confidence": 0.88},
>     {"category": "物品", "name": "海洋", "confidence": 0.92}
>   ],
>   "description": "一段50字以内的照片描述"
> }
> ```

> **人物识别**
> ```
> 请分析这张照片中的人物信息：
> 1. 检测照片中的人物数量（0表示无人脸）
> 2. 人物在画面中的占比（主体/背景/边缘）
> 3. 人物状态（正面/侧面/背面/遮挡）
> 4. 表情（开心/平静/严肃/悲伤/惊讶等）
> 5. 是否有自拍特征（举起手机/对着镜头笑等）
>
> 请以JSON格式输出：
> {
>   "person_count": 3,
>   "person_ratio": 0.6,
>   "person_position": "主体",
>   "expressions": ["开心", "平静", "开心"],
>   "is_selfie": false,
>   "face_clarity": 0.85,
>   "summary": "3人合影，主体占据60%画面，表情开心"
> }
> ```

> **地理位置分析**
> ```
> 请分析这张照片的地理位置信息（如果可从画面推断）：
> 1. 推断地点类型：城市/乡村/自然景观/室内/无法判断
> 2. 可能的地区特征：东方/西方/亚洲/欧洲/美洲等
> 3. 具体地标（如果可识别）
> 4. 旅行标记：是否像旅行中拍摄的照片
> 5. 地点稀有度（0-100）：该地点的罕见程度
>
> 请以JSON格式输出：
> {
>   "location_type": "自然景观",
>   "region特征": "亚洲",
>   "landmark": "富士山",
>   "is_travel": true,
>   "travel_confidence": 0.92,
>   "rarity_score": 75,
>   "summary": "日本富士山旅行照，地点稀有度较高"
> }
> ```

> **时间与事件分析**
> ```
> 请分析这张照片的时间信息和可能的事件：
> 1. 推断季节：春/夏/秋/冬/无法判断
> 2. 推断时间段：清晨/上午/中午/下午/傍晚/夜晚
> 3. 是否为特殊时刻：日出/日落/黄金时刻/蓝调时刻/特殊天象
> 4. 事件类型：日常/旅行/聚会/节日/纪念日/其他
> 5. 事件置信度（0-1）
>
> 请以JSON格式输出：
> {
>   "season": "夏",
>   "time_of_day": "下午",
>   "special_moment": "黄金时刻",
>   "event_type": "旅行",
>   "event_confidence": 0.85,
>   "event_name": "日本京都旅行",
>   "summary": "夏季下午黄金时刻，日本京都旅行照片"
> }
> ```

> **重复内容检测**
> ```
> 请分析这张照片，判断其与其他照片的相似度：
> 1. 场景唯一性（0-100）：该场景是否常见
> 2. 构图独特性（0-100）：构图是否有特色
> 3. 可能的重复组ID（如果有明显重复）
> 4. 在相似组中的质量评估：优/中/差
>
> 请以JSON格式输出：
> {
>   "scene_uniqueness": 70,
>   "composition_uniqueness": 65,
>   "duplicate_group_id": null,
>   "quality_in_group": null,
>   "similar_photos_exist": false,
>   "summary": "普通日常照片，场景有一定独特性"
> }
> ```

> **视频内容分析**
> ```
> 请分析这个视频片段的内容：
> 1. 视频类型：风景/人物/动物/美食/建筑/其他
> 2. 主要场景标签（最多5个）
> 3. 是否有重要事件（开始/中间/结尾）
> 4. 视频质量评分（0-100）：清晰度、稳定度、内容价值
> 5. 适合保留的原因（如果有）
>
> 请以JSON格式输出：
> {
>   "video_type": "风景",
>   "scene_tags": ["海边", "日落", "旅行"],
>   "has_significant_event": true,
>   "event_position": "结尾",
>   "quality_score": 88,
>   "retention_reason": "黄金时刻日落海边，画面质量优秀",
>   "summary": "海边日落风景视频，结尾有篝火场景"
> }
> ```

> **照片内容描述（≤300字）**
> ```
> 请在300字以内描述照片拍摄内容，并补充植物/动物/食物等对象的简短科普信息。
> 输出 JSON: {"description":"..."}
> ```

> **照片回忆（≤200字）**
> ```
> 请在200字以内生成照片回忆，包含：同行好友、时间、旅行第几天、城市、拍摄前后活动与拍摄时心情分析。
> 输出 JSON: {"diary":"..."}
> ```

**用户自定义提示词配置入口：**
```
设置 → AI 提示词 → [任务类型] → 编辑
```

#### 2.3.3 自定义分析任务（用户创建）

用户可创建完全自定义的分析任务，包括自定义提示词、结果示例、指标字段名。

**创建自定义任务流程：**

```
设置 → 自定义任务 → [+ 新建任务]
```

**自定义任务编辑器：**

```
┌────────────────────────────────────────────────────────┐
│  新建自定义分析任务                                     │
├────────────────────────────────────────────────────────┤
│                                                        │
│  任务名称: [ 我的旅行打卡评分________________]          │
│                                                        │
│  提示词内容:                                           │
│  ┌──────────────────────────────────────────────────┐ │
│  │ 请分析这张照片是否为旅行打卡照：                   │ │
│  │ 1. 是否包含地标建筑（0-100）                     │ │
│  │ 2. 打卡感是否强烈（0-100）                       │ │
│  │ 3. 画面是否干净（无杂乱背景）（0-100）             │ │
│  │ 请以JSON格式输出：                              │ │
│  │ {                                               │ │
│  │   "landmark_score": 85,                        │ │
│  │   "checkin_feeling": 90,                       │ │
│  │   "cleanliness": 80,                           │ │
│  │   "final_checkin_score": 85                    │ │
│  │ }                                              │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  结果示例（用于解析输出格式）:                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │ {                                               │ │
│  │   "landmark_score": 85,                        │ │
│  │   "checkin_feeling": 90,                       │ │
│  │   "cleanliness": 80,                           │ │
│  │   "final_checkin_score": 85                    │ │
│  │ }                                              │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  指标字段（用于数据库存储和筛选）:                      │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ✓ landmark_score      数值  0-100               │ │
│  │ ✓ checkin_feeling     数值  0-100               │ │
│  │ ✓ cleanliness         数值  0-100               │ │
│  │ ✓ final_checkin_score 数值  0-100 (作为主分)    │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │ + 添加指标字段                                  │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
│  启用此任务: [✅ 开启]                                  │
│                                                        │
│              [ 取消  ]  [ 保存 ]                       │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**自定义任务与 AI 筛选集成：**

自定义任务的指标字段会自动出现在 AI 筛选器中：

```
┌────────────────────────────────────────────────────────┐
│  AI 筛选                                               │
├────────────────────────────────────────────────────────┤
│                                                        │
│  内置指标:                                             │
│  ☑️ 综合评分 > [  70  ]                               │
│  ☑️ 美学评分 > [  60  ]                               │
│  ☑️ 情感评分 > [  50  ]                               │
│                                                        │
│  自定义指标:                                           │
│  ☑️ 旅行打卡分 > [  75  ]  (自定义任务: 我的旅行打卡)  │
│  ☑️ 地标分 > [  80  ]       (自定义任务: 我的旅行打卡) │
│                                                        │
│  [  应用筛选  ]                                        │
│                                                        │
└────────────────────────────────────────────────────────┘
```

#### 2.3.4 任务调度策略

| 配置项 | 产品要求 |
|--------|----------|
| 并行任务数 | 默认 2，可调 1~4（设置 → 分析设置） |
| 任务优先级 | P0（打分/标签）> P1（人物/地理/视频）> P2（重复检测） |
| 状态控制 | 支持暂停、继续、停止、失败重试 |
| 进度展示 | 菜单栏显示进度与已处理数量（侧边栏不再展示 AI 分析进度） |

> 调度器架构、Actor 模型、断点续传实现见 [TECH.md §2.1、§2.2](TECH.md)。

#### 2.3.5 首次启动策略

| 场景 | 产品行为 |
|------|----------|
| 库规模 &lt; 1000 | 直接全量分析 |
| 库规模 ≥ 1000 | 默认先分析当年，完成后询问是否继续历史照片 |
| 后台运行 | 不阻塞用户正常使用 Mac |

#### 2.3.6 断点续传

- 分析进度持久化，应用重启后从中断处继续
- 已完成资源不重复分析（除非用户主动触发重新分析）

> 持久化字段与状态机见 [TECH.md §2.2](TECH.md)。

### 2.4 照片评分系统

#### 2.4.1 双评分体系与评分维度（百分制）

**双评分定义：**

| 评分名称 | 定义 | 可配置性 |
|----------|------|----------|
| **美学评分** | 严格摄影美学分（截图固定 0 分） | ❌ 锁定任务，提示词不可修改 |
| **综合评分** | 基于六维加权计算的总分 | ✅ 权重可调 |

**综合评分默认权重（可自定义修改）：**

| 维度 | 默认权重 | 评分标准 | 是否可配置 |
|------|----------|----------|-----------|
| **美学维度（来源于美学评分）** | 30% | 构图（平衡/层次/主体）、清晰度、色彩（和谐/饱和）、光线（自然/曝光） | ✅（权重可调，分数源不可改） |
| **技术质量** | 30% | 清晰度、噪点、曝光准确、抖动程度 | ✅ |
| **内容价值** | 15% | 是否有意义（活动、旅行、运动、聚会、人物等） | ✅ |
| **情感价值** | 10% | 人物数量、人物亲密程度、人物表情、情感表达、回忆感 | ✅ |
| **稀有性** | 10% | 时间稀有程度、地点稀有程度 | ✅ |
| **独特性** | 5% | 与其他照片的重复度（越独特越高） | ✅ |

> **权重配置入口：** 设置 → 评分权重 → 各维度滑块（0%~100%，总和需等于100%）

**关系约束：**

- 每张照片持有「综合评分」与「美学评分」两种分值；卡片角标默认展示美学评分，可在照片页工具栏的分段控件中一键切换为综合评分（无需进入设置页）
- 综合评分中的美学维度分值必须来自「美学评分（严格）」任务输出，不允许由综合评分任务重算

**权重修改后重新打分流程：**

```
用户修改权重 → 提示："权重已修改，是否对照片/视频重新打分？"
              → [ 重新打分全部 ] [ 仅重新打分的照片/视频 ] [ 暂不重新打分 ]
              → 重新打分后覆盖原结果
```

#### 2.4.2 重复照片降分机制

- 系统自动识别相似/重复照片并分组
- 组内保留质量最高的一张，其余照片综合分显著降低并标记为重复
- 用户可在「查找」浮层通过「重复/相似」芯片筛选

> pHash 算法、降分公式与数据库字段见 [TECH.md §2.4](TECH.md)。

#### 2.4.3 一键筛选删除

**删除流程（三步确认）：**

```
Step 1: 用户设置分数滑块（0-100可调，可手动输入）
Step 2: 点击"预览" → 显示符合条件的所有照片
Step 3: 点击"移至废纸篓" → 弹出二次确认对话框
        ┌────────────────────────────────────────┐
        │  确认删除                               │
        │  ───────────────────────────────────── │
        │  即将删除 1,234 张照片                  │
        │  这些照片将移至系统废纸篓               │
        │                                        │
        │  ☑️ 我确认要删除这些照片               │
        │                                        │
        │        [ 取消  ]  [ 确认删除 ]         │
        └────────────────────────────────────────┘
Step 4: 确认后执行删除（调用 Photos Framework API）
```

**筛选器界面（Demo 已实现核心交互）：**

```
┌────────────────────────────────────────────────────────┐
│  筛选                                                  │
├────────────────────────────────────────────────────────┤
│  维度评分范围（7维）                                     │
│  [美学][综合][技术][内容][情感][稀有][独特] ← 维度切换    │
│  [ 大于 ] [ 小于 ]   ← 比较运算符切换                   │
│  0 [=======●========] 100   [ 60 ]                     │
│  只显示美学评分大于 60 分的照片（默认）                    │
│                                                        │
│  时间范围（天/周/月/年分排 + 特殊选项，见 8.4）          │
│                                                        │
│  设备与来源: [专业设备] [专业软件调整]                   │
│  其他筛选:   [重复/相似] [近一月新增] [已存iCloud]       │
│                                                        │
│  预览 + 移至废纸篓（见 8.3）                             │
└────────────────────────────────────────────────────────┘
```

**评分比较规则：**

| 规则 | 说明 |
|------|------|
| 维度切换 | 可在美学/技术/内容/情感/稀有/独特中任选一个维度，默认美学 |
| 大于 | `selected_dimension_score > 阈值` |
| 小于 | `selected_dimension_score < 阈值` |

- 滑块与数字输入框**双向同步**（0–100）
- 下方提示文案随运算符和维度实时更新，例如「只显示技术质量**小于** 60 分的照片」

### 2.5 标签系统

#### 2.5.1 自动标签分类

| 标签大类 | 示例标签 |
|----------|----------|
| **场景** | 海滩、山景、城市、乡村、夜景、室内、室外、森林、沙漠 |
| **物品** | 食物、动物、植物、汽车、建筑、天空、云朵、海洋、书籍 |
| **人物** | 人像、合影、自拍、背影、孩童、宠物、多人、单人 |
| **活动** | 旅行、聚餐、运动、婚礼、生日、会议、演出、购物 |
| **时间** | 日出、日落、正午、傍晚、夜晚、黄金时刻、蓝调时刻 |
| **天气** | 晴天、多云、雨天、雪天、雾天、雷电 |
| **情绪** | 开心、悲伤、浪漫、宁静、热闹、庄严、神秘 |

#### 2.5.2 自定义标签

- 用户可添加自定义标签规则
- 支持正则表达式匹配
- 支持按时间/地点自动打标签

### 2.6 搜索功能

#### 2.6.1 搜索方式

| 搜索类型 | 示例 |
|----------|------|
| **自然语言** | "去年夏天在海边拍的照片"、"有狗狗的照片" |
| **标签筛选** | 场景:海滩 + 人物:宠物 |
| **分数筛选** | 分数 > 80 |
| **时间筛选** | 2025年春节、2024年夏天 |
| **地点筛选** | 北京、上海、日本 |
| **AI 筛选** | 综合评分>70 AND 情感评分>60 AND 稀有性>50 |
| **组合筛选** | 场景:海滩 AND 分数:>70 AND 时间:2025 |

#### 2.6.2 搜索结果展示

- 网格视图：大图预览
- 列表视图：缩略图 + 关键信息
- 支持结果排序（时间、分数、相关度）

#### 2.6.3 全局「查找」浮层（v0.0.11 重构，统一搜索与筛选）

> **重构说明：** 原「搜索页」与「筛选页」已于 v0.0.11 合并为单一的全局「查找」浮层。自然语言查询与属性筛选芯片以 **AND** 叠加，结果实时作用于照片墙。详细交互见 **§8.12**。

**设计目标：**

- 用一句话或若干芯片快速找回记忆，不要求用户理解筛选器语法
- 让查找成为“入口能力”：随处可唤起、保留当前浏览上下文、所见即所得
- 让状态可见：当前条件摘要、匹配数量实时反馈

**入口设计：**

| 入口 | 触发方式 | 预期行为 |
|------|----------|----------|
| 顶栏居中搜索胶囊 | 点击「搜索照片…」 | 唤起查找浮层并聚焦输入框 |
| 底部导航「查找」 | 点击导航项 | 同上 |
| 快捷键 | `⌘K` / `Ctrl+K` | 任意页面唤起/关闭查找浮层 |

**交互流程：**

```
唤起查找浮层 → 输入自然语言 / 勾选属性芯片 / 调整评分条件
            → 浮层顶部实时显示「匹配 N 张」
            → 身后照片墙实时更新结果（无需预览/应用按钮）
            → 关闭浮层：有条件则保留生效并显示横幅，无条件则恢复全部
```

**查询解析优先级（Demo 规则）：**

1. **显式分数条件**（如“综合评分大于80”、“美学评分>85”）
2. **时间语义**（今年/去年/近一月）
3. **标签语义**（旅行/宠物/建筑/美食等）
4. **地点词匹配**（北京/上海/厦门等）

**关键状态与空态：**

- 无条件：浮层显示「全部 N 张」，照片墙展示全部
- 有条件但无结果：照片墙空集，浮层显示「匹配 0 张」，可一键「重置」
- 有结果：浮层显示匹配数量与条件摘要，照片墙同步更新

### 2.7 照片管理

| 功能 | 描述 |
|------|------|
| **移至废纸篓** | 两步确认后，将照片移至系统废纸篓 |
| **恢复照片** | 从废纸篓恢复 |
| **批量操作** | 批量选择、批量删除、批量打标签 |
| **单张详情** | 查看维度分、标签、时间地点、相似组与保留建议 |
| **收藏** | 标记为收藏，保护不被删除建议覆盖 |
| **隐藏分析** | 将某些照片排除在分析队列外 |

### 2.8 分析报告

#### 2.8.1 报告存储

- 格式：**Markdown（.md）**
- 存储位置：**SQLite 数据库**（photos_reports 表）
- 报告内容：分析摘要、统计信息、重要发现

#### 2.8.2 报告管理

```
┌────────────────────────────────────────────────────────┐
│  分析报告                                              │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │ 📄 2026-05-25 首次分析报告      2026-05-25   │   │
│  │    照片 45,678 张 | 视频 3,214 个             │   │
│  │    平均分 68.5 | 废片建议 12,345 张           │   │
│  │    [ 查看 ] [ 删除 ]                         │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │ 📄 2026-05-20 周度报告          2026-05-20   │   │
│  │    照片 1,234 张 | 视频 89 个                 │   │
│  │    [ 查看 ] [ 删除 ]                         │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
│  [+ 生成新报告]                                        │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**查看报告：**
- 在软件内嵌的 Markdown 阅读器中查看
- 支持复制、导出到桌面

### 2.9 交互式 Demo 原型

为便于评审 UI/UX，项目提供可浏览器打开的 HTML 原型：

| 路径 | 说明 |
|------|------|
| `demo/index.html` | 主界面：4 个 Tab + 全局「查找」浮层 + 侧边栏 + 弹窗 |
| `demo/css/style.css` | Liquid Glass 风格样式与 CSS 变量 |
| `demo/js/app.js` | 页面路由、筛选逻辑、配置弹窗等演示逻辑 |

**Demo 已覆盖的能力摘要：** 照片网格（7级 `−`/`+` 缩放，丝滑过渡；工具栏分段控件切换角标评分维度；4选项联动排序）、侧边栏 overlay 模式（磨砂玻璃叠加照片上方，滚动 >10px 自动隐藏，停滚恢复；8个默认标签，更多/收起带图标）、照片详情弹窗（维度分/标签/地点时间/相似组/保留建议/内容描述/照片回忆）、时间线按年浏览、**全局「查找」浮层（自然语言 + 媒体类型/评分/时间/状态来源/设备/地点/人物/标签芯片，实时更新照片墙）**、报告列表、设置（模型/权重/提示词）、4 页导航 + 全局查找、三态主题（跟随系统/浅色/深色）。

**完整交互清单见第八章。** 正式版实现映射见 [TECH.md §7.2](TECH.md)。

---

## 三、相关技术文档

技术架构、数据库设计、模块实现、性能优化、开发任务与计划已独立成文：

| 文档 | 内容 |
|------|------|
| **[TECH.md](TECH.md)** | 技术栈、SQLite 表结构、TaskScheduler/ScoringEngine 设计、MLX-VLM 集成、性能策略、Phase 1–9 TODO、开发估算、风险应对 |

---

## 四、UI 设计

### 4.1 设计语言：Apple Liquid Glass（液态玻璃）

**macOS Tahoe 最新设计语言特性：**

| 特性 | 说明 |
|------|------|
| **毛玻璃背景** | 使用 `.glass` / `.ultraThinMaterial` 系列背景 |
| **半透明窗口** | 窗口背景半透明，呈现深度感 |
| **柔和边框** | 边框极细（0.5pt），颜色淡雅 |
| **动态光效** | 模拟光线折射效果 |
| **模糊层次** | 背景模糊强度分级（ultraThin/thin/regular/thick） |
| **圆角半径** | 大圆角（16-24pt）营造柔和感 |

**色彩配置（适配 Light/Dark Mode）：**

| 元素 | Light Mode | Dark Mode |
|------|-----------|-----------|
| 窗口背景 | `.ultraThinMaterial` | `.ultraThinMaterial` |
| 侧边栏背景 | `.sidebar` | `.sidebar` |
| 卡片背景 | `.secondaryUltraThinMaterial` | `.secondaryUltraThinMaterial` |
| 主色调 | System Blue | System Blue |
| 强调色 | System Indigo | System Indigo |
| 文字主色 | System Label | System Label |
| 文字次色 | System Secondary Label | System Secondary Label |

### 4.2 窗口结构（与 Demo 对齐）

```
┌──────────────────────────────────────────────────────────────────┐
│ MantaPhotos [照片][时间线][报告][设置]  ( 🔍 搜索照片… ⌘K )   ◐ 🔔 │
├──────────────┬───────────────────────────────────────────────────┤
│  侧边栏       │  主内容区（当前 Tab 对应页面）                      │
│  (仅照片页)   │                                                   │
│              │                                                   │
│  标签筛选     │                                                   │
│  快捷操作     │                                                   │
│  AI 分析进度  │                                                   │
│              │                                                   │
├──────────────┴───────────────────────────────────────────────────┤
│  [照片] [时间线] [🔍 查找] [报告] [设置] ← 底部液态玻璃导航（4页+查找） │
└──────────────────────────────────────────────────────────────────┘
        ┌──────────── 全局「查找」浮层（⌘K 唤起，覆盖任意页面） ────────────┐
        │ 🔍 一句话查找…                              [重置] [×]            │
        │ 匹配 N 张 · 照片页已实时更新                                     │
        │ 媒体类型 / 评分 / 时间范围 / 状态与来源 / 地点 / 人物 / 标签       │
        └────────────────────────────────────────────────────────────────┘
```

**导航规则（v0.0.11 更新）：**

| 规则 | 说明 |
|------|------|
| **主导航 4 项** | 顶部 Tab 与底部导航保留 照片 / 时间线 / 报告 / 设置，一一对应同步高亮 |
| **查找为全局浮层** | 不再是独立页面：顶栏居中搜索胶囊、底部「查找」图标、`⌘K` 均可唤起，覆盖当前页面而不跳转 |
| **侧边栏显隐** | 仅在「照片」页显示侧边栏及折叠按钮；其他页面自动隐藏；查找浮层开启时临时隐藏侧边栏以提供干净的实时画布 |
| **侧边栏折叠** | 左上角按钮可折叠/展开，图标在 `chevron-left` / `chevron-right` 间切换 |
| **主题三态** | 顶栏右侧按钮循环 跟随系统 / 浅色 / 深色，默认跟随系统 |

> **说明：** 「查找」浮层与照片详情弹窗已在 Demo 中落地（见 8.12、8.13）；自定义任务编辑器仍为后续版本重点。

### 4.3 主要视图

| 视图 | Demo 状态 | 说明 |
|------|-----------|------|
| **照片页** | ✅ 已实现 | 网格展示（单分角标，工具栏分段控件即时切换美学/综合）、排序、AI 分析入口、多选；查找条件实时作用于本页 |
| **时间线页** | ✅ Demo 已实现 / 🚧 Mac V1 仅导航入口 | 按年月事件轴、年份切换、旅行角标、地图视图（占位）；Demo 功能仍在持续调整优化，Mac 应用完整时间线功能待 Demo 设计稳定后再实现 |
| **报告页** | ✅ 已实现 | 报告列表、生成/查看/删除（演示数据） |
| **设置页** | ✅ 已实现 | 模型列表、导入、并行数、首年策略、权重/提示词弹窗 |
| **全局「查找」浮层** | ✅ 已实现 | 统一搜索+筛选：自然语言 + 媒体类型/评分/时间/状态来源/设备/地点/人物/标签芯片，实时更新照片墙（见 8.12，替代原搜索页与筛选页） |
| **照片详情弹窗** | ✅ 已实现 | 单张详情、双评分、相似组、保留建议、内容描述与照片回忆（见 8.13） |
| **自定义任务编辑器** | 📋 规划中 | 见 2.3.3 |

### 4.4 设计规范

| 设计元素 | 规范 |
|----------|------|
| **设计风格** | Apple Human Interface Guidelines + Liquid Glass |
| **颜色方案** | 支持 Light/Dark Mode，自动跟随系统 |
| **字体** | SF Pro（系统默认） |
| **图标** | SF Symbols |
| **圆角** | 卡片 16pt，窗口 12pt，按钮 8pt |
| **间距** | 8pt 网格系统 |
| **毛玻璃** | `.ultraThinMaterial` / `.regularMaterial` |
| **动作层级** | 每页保留一个主动作，次动作收敛到工具栏/弹窗 |
| **关键流程** | 高风险操作遵循「预览 → 确认 → 执行」闭环 |

### 4.5 交互哲学（产品评审结论）

- **聚焦而非堆叠：** 每个页面只解决一个核心问题（照片浏览、时间回忆、筛选决策、报告复盘、设置调优）。
- **可见的状态：** 让用户始终知道系统“现在处于什么状态”（当前筛选、当前模型、当前策略、当前选中数量）。
- **先预览再破坏：** 删除等不可逆动作必须先展示结果规模，再进入确认。
- **默认即最佳：** 默认模型、默认权重、默认年份应直接可用，减少首次学习成本。

---

## 五、文档目录结构

```
MantaPhotos/
├── SPEC.md                    # 产品需求与交互规格（本文档）
├── TECH.md                    # 技术设计与实现规格
├── README.md                  # 项目介绍
├── demo/                      # 可交互 HTML 原型（见 SPEC 第 2.9、第八章）
├── docs/                      # 扩展设计文档（待创建）
├── src/                       # 项目代码（待创建）
└── assets/                    # 设计资源（待创建）
```

---

## 六、评审要点

评审时确认以下内容：

1. ✅ 产品功能是否完整覆盖需求？
2. ✅ 评分权重与提示词配置是否符合预期？
3. ✅ 自定义分析任务机制是否满足需求？
4. ✅ UI 设计（液态玻璃）与 Demo 交互是否符合预期？

技术评审见 [TECH.md](TECH.md)：选型、数据库、开发计划与风险。

---

## 七、附录

> HuggingFace 模型下载地址见 [TECH.md §7.1](TECH.md)。

### 7.1 默认评分权重（可直接复制）

| 维度 | 字段名 | 默认权重 |
|------|--------|----------|
| 美学评分 | aesthetics_score | 0.30 |
| 技术质量 | technical_score | 0.30 |
| 内容价值 | content_score | 0.15 |
| 情感价值 | emotion_score | 0.10 |
| 稀有性 | rarity_score | 0.10 |
| 独特性 | uniqueness_score | 0.05 |

### 7.2 内置提示词（Demo 默认任务）

Demo 设置页「AI 提示词配置」中内置 7 项任务（其中 1 项锁定不可编辑）：

| 任务 ID | 名称 | 默认提示词摘要 |
|---------|------|----------------|
| `score` | 综合评分 | 汇总多维度分值，计算 final_score |
| `aestheticsStrict` | 美学评分（严格） | 摄影美学严格评分；截图固定 0 分；锁定不可编辑 |
| `tag` | 内容标签 | 场景/物品/人物/活动标签数组 |
| `person` | 人物识别 | 人物数量、表情、自拍特征 |
| `location` | 地理位置 | 地理位置、是否为旅行照 |
| `description` | 照片内容描述（≤300字） | 拍摄内容描述 + 科普信息 |
| `diary` | 照片回忆（≤200字） | 同行人/时间/城市/行程/心情叙事 |

### 7.3 文档版本记录

> 完整版本历史与里程碑说明见文档首部**变更日志**章节。

| 版本 | 说明 |
|------|------|
| v0.0.1 ~ v0.0.6 | 各页面规格逐步成稿（搜索/筛选/时间线/报告/设置/详情） |
| v0.0.7 | 与 Demo 实现对齐，评审版 |
| v0.0.8 | 角标切换移至照片页工具栏分段控件 |
| v0.0.9 | 工具栏重排、7级缩放、联动排序、侧边栏边缘标签 |
| **v0.0.10** | **🎯 小里程碑**：缩放修正、侧边栏 overlay、滚动隐藏、标签云 8 个 |
| v0.0.11 | 搜索/筛选合并为全局「查找」浮层、导航收敛 4 项、实时筛选照片墙、新增设备来源等筛选维度、评分实时条件、主题跟随系统、浮层可读性增强 |
| v0.0.12 | 评分角标四档马卡龙液态玻璃、分数区间拖拽滑块配置、分段控件再点隐藏角标、缩放卡顿与切档抖动修复、站点 favicon、底部功能栏开关 |

---

*文档由 Eliza (AI 助手) 生成，待 Venk 评审后开始开发。*

---

## 八、界面交互规范（Demo 实现版）

> 本章根据 [`demo/index.html`](demo/index.html)、[`demo/css/style.css`](demo/css/style.css)、[`demo/js/app.js`](demo/js/app.js) **反向整理**，描述当前可交互原型中已实现的产品行为。  
> 标注 **📋 规划** 的条目为规格已有、Demo 尚未实现。

### 8.0 应用壳层与全局交互

| 模块 | 实现状态 | 说明 |
|------|----------|------|
| 顶部栏 | ✅ | Logo、4 个 Tab、居中搜索胶囊、主题切换、通知（占位） |
| 底部导航 | ✅ | 4 页 + 查找入口，液态玻璃，与顶部 Tab 同步 |
| 全局「查找」浮层 | ✅ | `⌘K` / 顶栏搜索胶囊 / 底部查找图标唤起；统一搜索与筛选（见 8.12） |
| 侧边栏折叠 | ✅ | 仅照片页显示；`sidebarToggle` 折叠/展开；查找浮层开启时临时隐藏 |
| Toast 反馈 | ✅ | 右上角堆叠提示，约 3 秒自动消失 |
| Esc 关闭弹窗 | ✅ | 优先关闭删除确认/权重/提示词/照片详情等模态，其次关闭查找浮层 |
| 主题 | ✅ | 三态循环 跟随系统 / 浅色 / 深色（默认跟随系统，监听系统变化），图标 半圆/太阳/月亮 |

**视觉规范（CSS 变量）：**

- 主背景渐变 + 毛玻璃卡片（`--glass-bg`、`backdrop-filter: blur`）
- 圆角：`--radius-lg` 16px / `--radius-md` 12px / `--radius-sm` 8px
- 强调色：蓝紫渐变（`--accent-gradient-1` / `--accent-gradient-2`）

---

### 8.1 照片页

**页面结构：**

```
┌─ 照片 ─────────────────── 48 张照片 ──────────────────────────┐
│  [− +]  [📷 美学 | 📈 综合]  [排序▼]  [▶ AI 分析]              │
├──────┬─────────────────────────────────────────────────────────┤
│ 侧←→ │  [缩略图+单分角标(默认美学)] [缩略图] ...                 │
│ 边   │                                                         │
│ 栏   │                                                         │
└──────┴─────────────────────────────────────────────────────────┘
```

**工具栏控件（从左到右）：**

| 控件 | 类型 | 说明 |
|------|------|------|
| `[− +]` 缩放 | 两个图标按钮 | 调节每行列数（1/2/4/8/16/32），过渡丝滑 |
| `[📷 美学 \| 📈 综合]` | 分段控件 | 切换角标评分维度，同步影响"按分排序"维度 |
| 排序下拉 | Select | 4 选项：时间新→旧 / 旧→新 / 当前评分高→低 / 低→高 |
| AI 分析 | 主按钮 | 启动分析 |

**角标评分分段控件（交互设计决策）：**

| 方案 | 说明 | 结论 |
|------|------|------|
| 单开关（Toggle） | 隐藏另一个状态，用户需记忆"当前是什么" | ❌ |
| 独立按钮（两个） | 需要额外标签说明语境，占用较多空间 | ❌ |
| **分段控件（Segmented Control）** | 两个选项同时可见，激活态渐变高亮，零认知负担，一触即达 | ✅ 采用 |

**照片网格尺寸调节（7 级）：**

| 级别 | 每行列数 | 适用场景 |
|------|----------|----------|
| 1 | 2 | 横向大图比对 |
| 2 | 4 | 宽松网格浏览 |
| 3 | 6 | 标准网格 |
| 4（默认）| 8 | 日常快速浏览 |
| 5 | 12 | 快速扫描 |
| 6 | 16 | 批量筛选 |
| 7 | 32 | 全量缩览 |

- `−` = 缩小（增加列数），`+` = 放大（减少列数）；到达边界时自动禁用（透明度 28%）
- 缩放无卡顿：列宽即时切换（不再对 `width` 做过渡，避免 Flexbox 逐帧 reflow），网格以 `transform: scale(0.995→1) 0.1s`（锚定左上角，走合成层）作轻量反馈；圆角随列数缩放（2列 14px → 32列 2px）
- 切换列数不会引起工具栏横向抖动：`.main-content` 用 `overflow-y:scroll + scrollbar-gutter:stable` 始终预留滚动条槽位
- 缩放到最后两级（16/32 列）缩略图过小，自动隐藏评分角标

**排序联动规则：**

- 排序选项「按 [X] 评分排序」中的 X 随分段控件动态更新（美学 / 综合）
- 当按分排序时，切换分段控件 → 排序立即以新维度重新排列，并提示"排序已同步"

**侧边栏折叠（Jobs 视角设计）：**

> 控件应当在它所控制的事物旁边，而不是在远处的工具栏。

| 状态 | 折叠标签位置 | 图标 | 说明 |
|------|-------------|------|------|
| 侧边栏**展开** | 贴合侧边栏右边缘（left: 260px） | `‹` | 半透明胶囊，hover 加深 |
| 侧边栏**折叠** | 屏幕左侧边缘（left: 20px） | `›` | 平滑滑至左边缘，随时可点击展开 |

标签本身 `transition: left 0.32s cubic-bezier(0.4,0,0.2,1)` 跟随侧边栏滑动，体感与侧边栏同步移动。主内容区同步 `padding-left` 过渡，照片网格不被侧边栏遮挡。

| 功能 | 实现状态 | 交互说明 |
|------|----------|----------|
| 照片网格 | ✅ | Flexbox 布局，每张显示单一分数角标（默认美学评分） |
| 角标分数切换 | ✅ | 工具栏分段控件（美学 / 综合），即时切换；**再点当前项隐藏角标**，隐藏态保留当前维度弱高亮 |
| 网格尺寸调节 | ✅ | `−`/`+` 按钮，7级，列宽即时切换 + transform 轻量反馈（无卡顿） |
| 评分角标配色 | ✅ | 四档马卡龙液态玻璃，默认 蓝 100-80 / 绿 80-60 / 黄 60-40 / 粉 40-0；区间可在设置中拖拽滑块配置（互斥覆盖 0-100） |
| 照片计数 | ✅ | 标题旁显示「N 张照片」 |
| 排序 | ✅ | 4选项：时间新→旧 / 旧→新 / 当前评分高→低 / 低→高，评分维度跟随分段控件 |
| 侧边栏折叠标签 | ✅ | 右边缘胶囊标签，展开/折叠随侧边栏滑动，主内容区 padding 同步过渡 |
| AI 分析 | ✅ | 主按钮启动分析并同步侧边栏状态 |
| 查看详情 | ✅ | 点击照片卡片打开详情弹窗 |
| 多选勾选 | ✅ | 点击卡片右上角勾选框切换选中态，显示底部多选工具栏 |
| 多选工具栏 | ✅ | 已选数量；移动、收藏、删除 |
| 删除确认 | ✅ | 模态框显示删除数量，确认后真正移除照片 |

**侧边栏（仅照片页）：**

| 区块 | 内容 |
|------|------|
| **标签筛选** | 搜索框 + 标签云（默认标签）+「更多」展开扩展标签，点击即应用筛选 |
| **快捷操作** | 我的收藏、废片篓、高分照片，点击即切换预设筛选 |

**筛选协同规则：**

- 点击「全部」标签会清空快捷筛选状态，回到默认浏览集合
- 快捷筛选与「查找」浮层高级条件解耦：先选集合，再做高级筛选

---

### 8.2 时间线页

> **Mac 应用实现说明：** 时间线页 Demo 功能仍在持续调整和优化中，尚未达到稳定的最终版本。Mac 应用 V1 仅创建时间线导航菜单入口（占位），完整时间线原生功能待 Demo 设计完成并稳定后再进行开发。以下为 Demo 当前已实现的交互说明，供产品评审参考。

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 年份选择 | ✅ Demo | 下拉仅展示有照片的年份，默认优先当前年份（无数据则回退到最近有数据年份） |
| 年份切换 | ✅ Demo | 左右箭头切换年份并刷新列表 |
| 事件轴 | ✅ Demo | 按月聚合：日期、标题、横向照片条 |
| 旅行角标 | ✅ Demo | `isTravel: true` 的事件显示「✈ 旅行」药丸标签 |
| 空状态 | ✅ Demo | 该年无数据时提示「N 年暂无记录」 |
| 地图视图 | 📋 Demo 占位 | 按钮占位，Toast「地图视图开发中」 |

**时间线数据结构（Demo）：**

```typescript
interface TimelineEvent {
  date: string;       // 如 "2025年8月"
  title: string;      // 如 "三亚之旅"
  photos: string[];   // 缩略图 URL 列表
  isTravel: boolean;  // 是否旅行事件
}
```

**默认演示数据：**

| 年月 | 标题 | 旅行 |
|------|------|------|
| 2025年8月 | 三亚之旅 | ✅ |
| 2025年7月 | 张家界登山 | ✅ |
| 2025年6月 | 北京美食周 | — |
| 2025年5月 | 宠物日常 | — |
| 2024年12月 | 圣诞聚会 | — |
| 2024年10月 | 厦门秋游 | ✅ |

---

### 8.3 筛选（已并入全局「查找」浮层）

> **v0.0.11 变更：** 独立「筛选页」已移除，所有筛选维度并入全局「查找」浮层（见 **§8.12**）。删除动作从查找剥离，归口到多选工具栏与照片详情（见 8.9、8.13）。

**查找浮层中的筛选维度（顺序）：**

| 顺序 | 维度 | 实现状态 | 说明 |
|------|------|----------|------|
| 1 | **媒体类型** | ✅ | 照片 / 视频 / Live / 截图 / RAW（多选，含命中计数） |
| 2 | **评分** | ✅ | 单条实时条件，见下表 |
| 3 | **时间范围** | ✅ | 见 8.4 |
| 4 | **状态与来源** | ✅ | 收藏、重复/相似、近一月新增、已存 iCloud、专业设备、专业软件调整；**设备来源**新增一行：Pocket、运动相机、无人机、单反相机（多选，含计数与图标） |
| 5 | **地点** | ✅ | 地点芯片（多选，含计数） |
| 6 | **人物** | ✅ | 人物芯片（多选，含计数） |
| 7 | **标签** | ✅ | 标签芯片（多选，含计数） |

**评分条件交互（v0.0.11，单条实时生效）：**

| 控件 | 行为 |
|------|------|
| 维度单选标签 | 美学/综合/技术/内容/情感/稀有/独特，默认**美学** |
| 比较运算符 | 「大于」「小于」二选一，默认**大于** |
| 分数条（滑块） | 0–100，`oninput` 实时同步，按运算符方向渐变填充 |
| 数字框 | 手动输入，与滑块双向同步（0–100 钳制） |
| 提示文案 | 「只显示{维度}{大于\|小于} **N** 分的照片」；默认「美学 > 0」即不过滤 |
| 生效方式 | 切换维度/运算符/数值即时作用于照片墙，**无需「添加条件」按钮** |

**筛选逻辑（`computeFindFilter`）：** 自然语言查询与各属性芯片以 **AND** 叠加；评分条件 `getMetricScore(photo, metric) >|< value`；设备来源 `photo.deviceType ∈ devices`。

**应用规则（v0.0.11）：**

- 任一条件变化 → 浮层计数与身后照片墙实时更新，**无预览/应用中间态**
- 关闭浮层：有条件则保留生效并在照片页显示横幅，无条件则恢复全部

---

### 8.4 时间范围快捷筛选

- 按**天 / 周 / 月 / 年**分四排，每排左侧为日历类图标
- 点击芯片**单选**高亮（`active` 渐变样式）
- 选中后在底部 **已选范围条** 展示起止日期或特殊文案，支持 **× 清除**
- **特殊筛选**（紫色星标行）：最近一次旅行、法定节假日
- **全部时间** 单独一行，虚线边框样式；选中后隐藏已选范围条

| 分类 | 选项 |
|------|------|
| 天 | 今天、昨天、近3天 |
| 周 | 本周、近一周、上周、近两周 |
| 月 | 本月、近一月、上月、近3月、近6月 |
| 年 | 本年、去年、近两年 |
| 特殊 | 最近一次旅行（演示：2025-08-01 至 2025-08-15）、法定节假日（演示：春节·国庆·劳动节） |
| 全部 | 全部时间 |

**日期计算：** 以本地「今天」为基准动态计算；法定节假日为演示固定文案。

---

### 8.5 报告页

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 报告列表 | ✅ | 卡片：标题、日期、照片数、平均分 |
| 生成新报告 | ✅ UI | Toast 模拟生成流程 |
| 查看 | ✅ UI | Toast 打开报告 |
| 删除 | ✅ UI | Toast 删除单条 |

**报告类型（演示数据）：**

| 类型 | 示例标题 |
|------|----------|
| `initial` | 首次分析报告 |
| `weekly` | 周度分析报告 |
| `manual` | 旅行照片专项报告 |

**规划：** 内嵌 Markdown 阅读器、导出桌面（见 2.8.2）

---

### 8.6 设置页

#### 8.6.1 AI 模型

| 模型 | 标签 | 操作态（Demo） |
|------|------|----------------|
| Qwen2.5-VL-3B | 快速 | 可切换为使用中 |
| Qwen2.5-VL-7B | 均衡 | 可切换为使用中 |
| Qwen3-VL-4B | ⭐ 默认 | 初始使用中 |
| Qwen3-VL-8B | — | 可切换为使用中 |
| Qwen3-VL-30B-A3B | 旗舰 | 可切换为使用中 |

- **手动导入模型**：入口「从本地导入 MLX 模型文件」（演示 Toast）
- 存储路径见 2.2.1

#### 8.6.2 分析设置

| 配置项 | Demo 表现 |
|--------|-----------|
| 并行任务数 | 下拉 1–4，默认 2 |
| 首次分析策略 | 点击循环切换：仅今年 → 近三年 → 全量 |
| 评分权重配置 | 打开模态框（见 8.7） |
| AI 提示词配置 | 打开模态框（见 8.8） |
| 分数区间配色（v0.0.12） | 「分数标签」分组内：色条预览 + 3 个拖拽滑块手柄，控制四档（蓝/绿/黄/粉）分界，拖动实时刷新照片墙；含恢复默认（80/60/40） |
| 显示底部功能栏（v0.0.12） | 开关（默认开启），关闭后隐藏底部导航以扩大内容区 |

> **注：** 「照片角标分数显示」已从设置页移至**照片页工具栏分段控件**（再点当前项隐藏），见 8.1。

---

### 8.7 评分权重配置弹窗

| 功能 | 说明 |
|------|------|
| 六维滑块 | 美学 30%、技术 30%、内容 15%、情感 10%、稀有 10%、独特 5% |
| 分值来源约束 | 美学维度分值固定来自「美学评分（严格）」任务输出（截图=0） |
| 实时总和 | 显示权重总和，≠100% 时警告并禁止保存 |
| 恢复默认 | 一键恢复上述默认比例 |
| 保存 | 总和=100% 时可保存，Toast 提示 |

与 **2.4.1**、**7.1** 字段一致；`weightConfig` 数组驱动 UI 渲染。

---

### 8.8 AI 提示词配置弹窗

| 功能 | 说明 |
|------|------|
| 任务列表 | 7 个内置任务；其中「美学评分（严格）」带锁定标识 |
| 展开编辑 | 非锁定任务可「编辑」展开 `textarea` |
| 锁定规则 | 美学评分任务只读展示，提示词不可编辑、不可自定义 |
| 保存 / 取消 | 保存仅回写可编辑任务提示词并关闭弹窗；取消仅关闭 |

默认任务与文案见 **7.2**。

---

### 8.9 多选与删除（Demo 简化版）

**多选工具栏（选中 ≥1 张时自底部浮出）：**

| 按钮 | 功能 |
|------|------|
| 移动 | 演示 Toast |
| 收藏 | 演示 Toast |
| 删除 | 打开删除确认模态框 |

**删除确认（当前 Demo）：**

- 文案：「确定要删除选中的 N 张照片吗？此操作不可撤销。」
- 取消 / 确认删除
- 删除数量来自当前选择或筛选预览结果
- 确认后会从照片集合中移除，并刷新计数与网格

**规划增强（见 8.10）：** 照片/视频分计、强制勾选确认

---

### 8.10 规划中的交互（规格有、Demo 未实现）

| 模块 | 参考章节 |
|------|----------|
| 多选：全选、排除分析 | 2.7 |
| 删除确认：勾选框 + 视频统计 | 2.4.3 |
| 自定义分析任务编辑器 | 2.3.3 |
| iCloud 一键下载区块 | 2.2、备份版设置页 |
| 报告 Markdown 内嵌阅读器 | 2.8.2 |

---

### 8.11 底部导航与主题

**底部导航（4 页 + 查找入口）：**

| 图标 | 页面 ID | 名称 | 行为 |
|------|---------|------|------|
| `fa-images` | `photos` | 照片 | 切换页面 |
| `fa-clock` | `timeline` | 时间线 | 切换页面 |
| `fa-search` | —（`onclick=openFind`） | 查找 | 唤起全局查找浮层 |
| `fa-file-alt` | `reports` | 报告 | 切换页面 |
| `fa-cog` | `settings` | 设置 | 切换页面 |

**设计参数：** 高度约 64px、圆角 32px、`backdrop-filter: blur(30px)`，激活项高亮。

**主题（v0.0.11 三态）：** 顶栏右侧按钮循环 **跟随系统 → 浅色 → 深色**；进入主页默认「跟随系统」，并监听 `prefers-color-scheme` 变化实时切换；图标分别为 半圆（系统）/ 太阳（浅）/ 月亮（深），CSS 变量全局生效。

---

### 8.12 全局「查找」浮层（已实现，v0.0.11 统一搜索+筛选）

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 唤起 / 关闭 | ✅ | `⌘K` / 顶栏搜索胶囊 / 底部「查找」图标唤起；`Esc` 或点击空白关闭 |
| 自然语言输入 | ✅ | 实时解析（分数/时间/标签/地点），与属性芯片 AND 叠加；带清空按钮 |
| 实时计数 | ✅ | 浮层顶部显示「匹配/全部 N 张 · 照片页已实时更新」 |
| 照片墙实时更新 | ✅ | 任一条件变化即时刷新身后照片墙，无预览/应用按钮 |
| 媒体类型筛选 | ✅ | 照片/视频/Live/截图/RAW（多选，含计数） |
| 评分条件 | ✅ | 维度单选标签 + 大于/小于 + 分数条（滑块/数字双向同步、渐变视觉），实时生效，默认 美学>0 不过滤 |
| 时间范围 | ✅ | 天/周/月/年 + 特殊（旅行/节假日）快捷芯片，见 8.4 |
| 状态与来源 | ✅ | 收藏/重复相似/近一月新增/已存iCloud/专业设备/专业软件调整 + 设备来源行（Pocket/运动相机/无人机/单反相机） |
| 地点 / 人物 / 标签 | ✅ | 多选芯片，含命中计数 |
| 条件摘要 | ✅ | 浮层底部展示当前生效条件文案 |
| 一键重置 | ✅ | 「重置」清空全部条件并恢复默认（美学>0、无芯片选中） |
| 关闭后照片墙 | ✅ | 有条件则保留生效并显示横幅（可清除），无条件则恢复全部 |
| 可读性 | ✅ | 面板模糊 36px、背景较 `bg-secondary` 加深约 5%，遮挡层加深，确保内容清晰 |

**查询解析规则（Demo）：**

- 分数：识别“综合评分/美学评分 + 大于/小于/高于/低于/>/<”
- 时间：识别“今年/去年/20xx年/近一月”
- 语义标签：旅行、风景、人物、人像、建筑、美食、宠物、夜景
- 地点：北京、上海、厦门、南京、杭州、三亚、成都、西安、广州、深圳

**数据模型补充（Demo）：** 每张照片新增 `mediaType`（photo/video/live/screenshot/raw）、`people`（人物数组）、`deviceType`（手机/单反相机/运动相机/无人机/Pocket）以支撑上述筛选维度。

---

### 8.13 照片详情弹窗（已实现）

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 打开方式 | ✅ | 照片页点击卡片打开；搜索结果「定位」后自动打开 |
| 双评分展示 | ✅ | 详情标题显示「综合评分 + 美学评分」 |
| 维度分展示 | ✅ | 美学/技术/内容/情感/稀有/独特 6 维条形分值 |
| 截图判定规则 | ✅ | 判定为截图时，美学评分强制为 0，并在详情中标注 |
| 基础信息 | ✅ | 显示拍摄时间、地点、标签 |
| 内容描述 | ✅ | 300 字以内描述拍摄内容并补充科普信息 |
| 照片回忆 | ✅ | 200 字以内生成行程叙事与心情分析 |
| 相似组信息 | ✅ | 显示重复组 ID 与组内质量排名；非重复则显示独立保留提示 |
| 保留建议 | ✅ | 按分数与重复状态输出「保留/复核/清理」建议 |
| 快捷动作 | ✅ | 收藏/取消收藏、删除此照片（复用删除确认流程） |

**交互闭环：**

1. 点击照片 → 打开详情弹窗  
2. 查看维度分 + 相似组 + 保留建议  
3. 决策动作：收藏或删除  
4. 删除走统一确认模态框，执行后刷新照片集合

---

### 8.14 Demo 与正式版映射

见 [TECH.md §7.2](TECH.md)。

---

*本章节随 Demo 迭代更新；以 `demo/` 目录源码为准。*
