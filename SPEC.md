# MantaPhotos - 产品需求规格文档

> **文档版本:** v0.0.10
> **创建日期:** 2026-05-25  
> **最后更新:** 2026-05-28（v0.0.10）
> **作者:** Venk
> **状态:** 待评审  
> **交互原型:** [`demo/index.html`](demo/index.html)（HTML/CSS/JS，用于产品交互验证）  
> **技术规格:** [TECH.md](TECH.md)（架构、数据库、实现方案与开发计划）

---

## 变更日志

### 里程碑原则

> 每当版本号尾数达到 **10 的倍数**（如 v0.0.10、v0.0.20、v0.1.0…），即为一个**小里程碑**。  
> 小里程碑版本说明须包含：本阶段新增功能摘要、重点完成的交互设计、已修复的问题，以及对后续迭代的方向指引。

---

### 🎯 v0.0.10 小里程碑（2026-05-28）

**阶段：交互原型 Demo 完整版 · v0.0.1 → v0.0.10**

本里程碑完成了从规格草稿到完整可交互原型的首个完整迭代周期。

#### 新增功能

| 功能 | 版本 | 说明 |
|------|------|------|
| 照片详情弹窗 | v0.0.6 | 双评分 + 6维度分 + 相似组 + 保留建议 + 内容描述 + 照片回忆 |
| 自然语言搜索 | v0.0.3 | 分数/时间/标签/地点语义解析，热门词推荐，结果定位到照片页 |
| 时间范围筛选 | v0.0.3 | 天/周/月/年/特殊（旅行/节假日）分排芯片，单选 + 已选展示条 |
| 7维评分筛选 | v0.0.3 | 大于/小于 + 滑块/数字框双向同步，筛选预览 + 移至废纸篓 |
| 时间线页 | v0.0.4 | 按年月事件轴，年份切换，旅行角标，空状态 |
| AI 提示词配置弹窗 | v0.0.5 | 7 任务展开编辑，美学评分锁定不可编辑 |
| 评分权重配置弹窗 | v0.0.5 | 6维滑块，总和校验，恢复默认 |
| 照片角标分段控件 | v0.0.8 | 从设置页移至照片页工具栏，两选项同时可见（Segmented Control） |
| 工具栏缩放控件 | v0.0.9 | `−`/`+` 按钮，7级列数（2/4/6/8/12/16/32），照片 width 丝滑过渡 |
| 排序联动 | v0.0.9 | 4选项，按分排序维度跟随分段控件（美学/综合）动态更新 |
| 侧边栏边缘标签 | v0.0.9 | 贴合侧边栏右边缘的胶囊折叠标签，平滑滑动 |
| 侧边栏 overlay 模式 | v0.0.10 | 侧边栏叠加在照片上方，磨砂玻璃背景，不推开主内容区 |
| 滚动自动隐藏侧边栏 | v0.0.10 | 累计滚动 >10px 自动隐藏，停滚后恢复至滚动前状态 |
| 标签云默认 8 个 | v0.0.10 | 全部/风景/人物/建筑/美食/旅行/宠物/夜景，更多按钮带图标+样式 |

#### 重点交互设计决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 角标切换方式 | Segmented Control | 两选项同时可见，零认知负担，不需要进设置页 |
| 缩放按钮语义 | `−` = 缩小（更多列）, `+` = 放大（更少列） | 与系统惯例一致 |
| 侧边栏遮挡方式 | overlay，可挡住照片 | 不压缩内容区宽度，磨砂玻璃背景产生层次感 |
| 滚动隐藏策略 | 累积 >10px 隐藏，停滚恢复 | 避免误触，滚动结束后还原用户意图 |
| 更多标签按钮 | 图标+文字小胶囊 | 纯文字按钮缺乏视觉引导，图标让可点击性更明确 |

#### 已修复问题

| 问题 | 修复版本 | 说明 |
|------|----------|------|
| `−`/`+` 按钮 delta 反转 | v0.0.10 | 原代码 `−` 调用 `adjustGridSize(-1)` 导致放大，已对调 |
| 缩放按钮禁用条件反转 | v0.0.10 | `outBtn.disabled` 与 `inBtn.disabled` 边界条件互换修正 |
| 排序"按分"仅支持综合 | v0.0.9 | 改为跟随分段控件维度，支持美学/综合双模式 |
| 侧边栏折叠按钮位置不直觉 | v0.0.9 | 从顶部栏移至侧边栏右边缘，控件紧贴所控对象 |

#### 后续迭代方向（v0.0.11+）

- 照片页多选 → 批量删除走三步确认流程（含视频统计）
- 自定义分析任务编辑器
- 报告 Markdown 内嵌阅读器 + 导出桌面
- iCloud 一键下载区块

---

### 历史版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.0.1 | 2026-05-25 | 初版需求规格基线：产品目标、AI分析任务分类、双评分体系、核心数据结构确立。 |
| v0.0.2 | 2026-05-26 | 核心页面与交互规格成稿：照片网格、时间线、筛选、报告、设置页主流程。 |
| v0.0.3 | 2026-05-26 | 搜索与筛选规格成稿：自然语言搜索解析规则、7维评分筛选、时间范围分排芯片。 |
| v0.0.4 | 2026-05-27 | 时间线与报告规格成稿：年月事件轴、旅行标记、报告列表与 Markdown 查看方案。 |
| v0.0.5 | 2026-05-27 | 设置与自定义任务规格成稿：模型管理、权重配置弹窗、提示词弹窗、锁定任务规则。 |
| v0.0.6 | 2026-05-27 | 照片详情弹窗与双评分规格成稿：维度分条形图、相似组、保留建议、内容描述、照片回忆。 |
| v0.0.7 | 2026-05-28 | Demo 与规格对齐（评审版）：第八章交互规范从 Demo 反向整理，补全 Demo 能力摘要。 |
| v0.0.8 | 2026-05-28 | 照片角标分数切换从设置页迁移至照片页工具栏分段控件，减少用户进设置的操作路径。 |
| v0.0.9 | 2026-05-28 | 工具栏控件重排（缩放→分段→排序），7级网格缩放丝滑过渡，4选项联动排序，侧边栏边缘折叠标签。 |
| **v0.0.10** | **2026-05-28** | **🎯 小里程碑**：照片页交互首个完整迭代。修正 `−`/`+` 缩放语义与禁用逻辑；侧边栏重构为 overlay 模式（叠加在照片上，磨砂玻璃背景），tab 移至左侧与面板动画同步；滚动 >10px 自动隐藏、停滚后 0.5s 延迟恢复；悬浮触发（0.8s 可配置，0~5s 支持关闭）；标签云默认展示 8 个标签（增加宠物/夜景），更多按钮升级为图标胶囊样式。首个可独立演示的照片页交互闭环，v0.0.11 起将打磨其余页面。 |

---

## 一、产品概述

### 1.1 产品定位

**MantaPhotos** 是一款运行在 macOS Tahoe 26.5+ 上的本地 AI 照片管理应用。接入 Qwen 开源多模态大模型，对用户照片库（照片+视频）进行深度内容分析、打分、打标签，帮助用户快速删除废片、快速搜索珍贵记忆。

### 1.2 核心价值

| 痛点 | 解决方案 |
|------|----------|
| 照片太多，找不到想要的那张 | AI 自然语言搜索 |
| 废片占用空间，删除太慢 | 一键按分数筛选删除 |
| 重复照片占据空间 | AI 识别相似/重复照片 |
| 照片没有分类标签 | AI 自动打标签 |
| 历史照片太多，首次导入慢 | 分年分批，首次先跑当年 |

### 1.3 目标用户

- 有大量照片库（10万+ 照片/视频）的 Mac 用户
- 希望高效管理照片、释放存储空间的用户
- 对 AI 辅助照片管理有需求的用户

### 1.4 iCloud 照片库策略

**核心原则：优先使用本地库照片**

| 场景 | 处理策略 |
|------|----------|
| **本地照片库** | 直接分析，无缝使用 |
| **iCloud 照片库（已下载原件）** | 视为本地照片，直接分析 |
| **iCloud 照片库（仅保留原件）** | 应用启动时提示用户"需要下载原件后才能分析"，提供一键下载入口 |
| **iCloud 照片库（下载中）** | 监听下载完成事件，完成后自动加入分析队列 |

> **说明：** 应用不管理 iCloud 同步逻辑，仅读取 Photos Framework 中的资源状态。若照片为 iCloud 远程资源（`isLocallyAvailable == false`），应用不会自动触发下载，但会提示用户。

---

## 二、功能需求

### 2.1 照片库接入

| 功能 | 描述 |
|------|------|
| **系统照片库授权** | 通过 Photos Framework 访问用户照片库 |
| **照片资源读取** | 读取照片元数据（EXIF、时间、地点、尺寸等） |
| **视频资源读取** | 读取视频元数据（时长、分辨率、编码格式等） |
| **实时监听** | 监听照片库变化，增量更新索引 |
| **iCloud 状态检测** | 检测资源是否本地可用，未下载时提示用户 |

### 2.2 AI 模型管理

#### 2.2.1 支持模型列表

| 模型名称 | 参数量 | 类型 | 推荐场景 | 最低内存 |
|----------|--------|------|----------|----------|
| Qwen2.5-VL-3B | 3B | BF16/FP16 | 日常照片，轻量快速 | 8GB |
| Qwen2.5-VL-7B | 7B | BF16/FP16 | 高质量分析，较快 | 16GB |
| Qwen3-VL-4B | 4B | BF16/FP16 | ✅ **默认推荐**，新版模型 | 8GB |
| Qwen3-VL-8B | 8B | BF16/FP16 | 高质量分析 | 16GB |
| Qwen3-VL-30B-A3B | 30B (A3B MoE) | BF16/FP16 | 最高质量，A3B 专家路由 | 32GB (M3 Max) |

> **下载与存储：** 模型由用户手动触发下载，支持从本地导入 MLX 模型。格式、存储路径、HuggingFace 地址见 [TECH.md §1.3、§7.1](TECH.md)。

#### 2.2.2 模型下载管理

```
┌────────────────────────────────────────────────────────┐
│  设置 → AI 模型                                        │
├────────────────────────────────────────────────────────┤
│                                                        │
│  当前使用: Qwen3-VL-4B ✅ 已下载                       │
│                                                        │
│  可用模型:                                            │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Qwen2.5-VL-3B          [已下载 ▼]                 │ │
│  │ Qwen2.5-VL-7B          [下载 215MB]              │ │
│  │ Qwen3-VL-4B ⭐默认     [已下载 ▼]               │ │
│  │ Qwen3-VL-8B            [下载 430MB]              │ │
│  │ Qwen3-VL-30B-A3B       [下载 1.2GB]              │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  手动导入模型...                                       │
│                                                        │
│  下载位置: ~/Library/Application Support/             │
│            MantaPhotos/Models/                   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### 2.3 后台 AI 分析任务

#### 2.3.1 分析任务类型

| 任务类型 | 描述 | 默认启用 | 并行优先级 |
|----------|------|----------|-----------|
| **综合评分** | 汇总多维度分数并输出综合分 0-100 | ✅ | P0 |
| **美学评分（严格）** | 仅摄影美学维度评分；截图内容固定 0 分 | ✅ | P0 |
| **内容标签** | 场景、物品、动作等标签 | ✅ | P0 |
| **人物识别** | 检测人脸数量、人物数量 | ✅ | P1 |
| **地理位置分析** | 解析地点，标记旅行事件 | ✅ | P1 |
| **照片内容描述（≤300字）** | 描述拍摄主体与场景，并补充科普信息 | ✅ | P1 |
| **照片回忆（≤200字）** | 生成当日叙事（日程、同行人、心情） | ✅ | P1 |
| **时间与事件分析** | 拍摄时间轴、旅行事件串联 | ✅ | P1 |
| **重复内容检测** | 检测相似/重复照片 | ✅ | P2 |
| **视频内容分析** | 视频关键帧抽取 + 内容分析 | ✅ | P1 |

**提示词策略：** 除「美学评分（严格）」外，其余内置任务支持在设置中调整提示词（详见 2.3.2）。

#### 2.3.2 自定义提示词配置

每个分析任务提供**系统默认提示词**。  
其中「美学评分（严格）」为**系统锁定任务**：提示词不可编辑、不可替换、不可自定义。

**系统默认提示词（内置生成）：**

> **综合评分**
> ```
> 你是照片质量评估助手。请接收以下输入：
> 1) 美学评分任务输出的 aesthetic_score（严格任务结果）
> 2) 其他维度分数：technical/content/emotion/rarity/uniqueness
>
> 按当前权重计算综合评分 final_score（0-100），并给出一句总结。
>
> 请以JSON格式输出：
> {
>   "aesthetics_score": 85,      // 仅引用严格美学评分结果，不在本任务重算
>   "technical_score": 80,       // 清晰度等技术分
>   "content_score": 90,         // 内容价值分
>   "emotion_score": 75,         // 情感表达分
>   "rarity_score": 60,          // 稀有性分
>   "uniqueness_score": 70,      // 独特性分
>   "final_score": 80,           // 综合评分（加权平均）
>   "summary": "简要评价"        // 简短评价
> }
> ```

> **美学评分（严格，系统锁定）**
> ```
> 你是严格摄影美学评审员，仅依据摄影美学打分（构图、光线、色彩、主体层次）。
> 规则：若输入图片为截图/录屏/界面截取内容，aesthetic_score 必须为 0，不可例外。
>
> 请以JSON格式输出：
> {
>   "aesthetic_score": 0-100,
>   "is_screenshot": true/false,
>   "reason": "一句话原因"
> }
> ```

> **内容标签**
> ```
> 请分析这张照片的内容，输出标签。
> 标签分类：
> - 场景：海滩、山景、城市、乡村、夜景、室内、室外、森林、沙漠等
> - 物品：食物、动物、植物、汽车、建筑、天空、云朵、海洋、书籍等
> - 人物：人像、合影、自拍、背影、孩童、宠物、多人、单人等
> - 活动：旅行、聚餐、运动、婚礼、生日、会议、演出、购物等
> - 时间：日出、日落、正午、傍晚、夜晚、黄金时刻、蓝调时刻等
> - 天气：晴天、多云、雨天、雪天、雾天、雷电等
> - 情绪：开心、悲伤、浪漫、宁静、热闹、庄严、神秘等
>
> 请以JSON格式输出：
> {
>   "tags": [
>     {"category": "场景", "name": "海滩", "confidence": 0.95},
>     {"category": "活动", "name": "旅行", "confidence": 0.88},
>     {"category": "物品", "name": "海洋", "confidence": 0.92}
>   ],
>   "description": "一段50字以内的照片描述"
> }
> ```

> **人物识别**
> ```
> 请分析这张照片中的人物信息：
> 1. 检测照片中的人物数量（0表示无人脸）
> 2. 人物在画面中的占比（主体/背景/边缘）
> 3. 人物状态（正面/侧面/背面/遮挡）
> 4. 表情（开心/平静/严肃/悲伤/惊讶等）
> 5. 是否有自拍特征（举起手机/对着镜头笑等）
>
> 请以JSON格式输出：
> {
>   "person_count": 3,
>   "person_ratio": 0.6,
>   "person_position": "主体",
>   "expressions": ["开心", "平静", "开心"],
>   "is_selfie": false,
>   "face_clarity": 0.85,
>   "summary": "3人合影，主体占据60%画面，表情开心"
> }
> ```

> **地理位置分析**
> ```
> 请分析这张照片的地理位置信息（如果可从画面推断）：
> 1. 推断地点类型：城市/乡村/自然景观/室内/无法判断
> 2. 可能的地区特征：东方/西方/亚洲/欧洲/美洲等
> 3. 具体地标（如果可识别）
> 4. 旅行标记：是否像旅行中拍摄的照片
> 5. 地点稀有度（0-100）：该地点的罕见程度
>
> 请以JSON格式输出：
> {
>   "location_type": "自然景观",
>   "region特征": "亚洲",
>   "landmark": "富士山",
>   "is_travel": true,
>   "travel_confidence": 0.92,
>   "rarity_score": 75,
>   "summary": "日本富士山旅行照，地点稀有度较高"
> }
> ```

> **时间与事件分析**
> ```
> 请分析这张照片的时间信息和可能的事件：
> 1. 推断季节：春/夏/秋/冬/无法判断
> 2. 推断时间段：清晨/上午/中午/下午/傍晚/夜晚
> 3. 是否为特殊时刻：日出/日落/黄金时刻/蓝调时刻/特殊天象
> 4. 事件类型：日常/旅行/聚会/节日/纪念日/其他
> 5. 事件置信度（0-1）
>
> 请以JSON格式输出：
> {
>   "season": "夏",
>   "time_of_day": "下午",
>   "special_moment": "黄金时刻",
>   "event_type": "旅行",
>   "event_confidence": 0.85,
>   "event_name": "日本京都旅行",
>   "summary": "夏季下午黄金时刻，日本京都旅行照片"
> }
> ```

> **重复内容检测**
> ```
> 请分析这张照片，判断其与其他照片的相似度：
> 1. 场景唯一性（0-100）：该场景是否常见
> 2. 构图独特性（0-100）：构图是否有特色
> 3. 可能的重复组ID（如果有明显重复）
> 4. 在相似组中的质量评估：优/中/差
>
> 请以JSON格式输出：
> {
>   "scene_uniqueness": 70,
>   "composition_uniqueness": 65,
>   "duplicate_group_id": null,
>   "quality_in_group": null,
>   "similar_photos_exist": false,
>   "summary": "普通日常照片，场景有一定独特性"
> }
> ```

> **视频内容分析**
> ```
> 请分析这个视频片段的内容：
> 1. 视频类型：风景/人物/动物/美食/建筑/其他
> 2. 主要场景标签（最多5个）
> 3. 是否有重要事件（开始/中间/结尾）
> 4. 视频质量评分（0-100）：清晰度、稳定度、内容价值
> 5. 适合保留的原因（如果有）
>
> 请以JSON格式输出：
> {
>   "video_type": "风景",
>   "scene_tags": ["海边", "日落", "旅行"],
>   "has_significant_event": true,
>   "event_position": "结尾",
>   "quality_score": 88,
>   "retention_reason": "黄金时刻日落海边，画面质量优秀",
>   "summary": "海边日落风景视频，结尾有篝火场景"
> }
> ```

> **照片内容描述（≤300字）**
> ```
> 请在300字以内描述照片拍摄内容，并补充植物/动物/食物等对象的简短科普信息。
> 输出 JSON: {"description":"..."}
> ```

> **照片回忆（≤200字）**
> ```
> 请在200字以内生成照片回忆，包含：同行好友、时间、旅行第几天、城市、拍摄前后活动与拍摄时心情分析。
> 输出 JSON: {"diary":"..."}
> ```

**用户自定义提示词配置入口：**
```
设置 → AI 提示词 → [任务类型] → 编辑
```

#### 2.3.3 自定义分析任务（用户创建）

用户可创建完全自定义的分析任务，包括自定义提示词、结果示例、指标字段名。

**创建自定义任务流程：**

```
设置 → 自定义任务 → [+ 新建任务]
```

**自定义任务编辑器：**

```
┌────────────────────────────────────────────────────────┐
│  新建自定义分析任务                                     │
├────────────────────────────────────────────────────────┤
│                                                        │
│  任务名称: [ 我的旅行打卡评分________________]          │
│                                                        │
│  提示词内容:                                           │
│  ┌──────────────────────────────────────────────────┐ │
│  │ 请分析这张照片是否为旅行打卡照：                   │ │
│  │ 1. 是否包含地标建筑（0-100）                     │ │
│  │ 2. 打卡感是否强烈（0-100）                       │ │
│  │ 3. 画面是否干净（无杂乱背景）（0-100）             │ │
│  │ 请以JSON格式输出：                              │ │
│  │ {                                               │ │
│  │   "landmark_score": 85,                        │ │
│  │   "checkin_feeling": 90,                       │ │
│  │   "cleanliness": 80,                           │ │
│  │   "final_checkin_score": 85                    │ │
│  │ }                                              │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  结果示例（用于解析输出格式）:                          │
│  ┌──────────────────────────────────────────────────┐ │
│  │ {                                               │ │
│  │   "landmark_score": 85,                        │ │
│  │   "checkin_feeling": 90,                       │ │
│  │   "cleanliness": 80,                           │ │
│  │   "final_checkin_score": 85                    │ │
│  │ }                                              │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  指标字段（用于数据库存储和筛选）:                      │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ✓ landmark_score      数值  0-100               │ │
│  │ ✓ checkin_feeling     数值  0-100               │ │
│  │ ✓ cleanliness         数值  0-100               │ │
│  │ ✓ final_checkin_score 数值  0-100 (作为主分)    │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │ + 添加指标字段                                  │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
│  启用此任务: [✅ 开启]                                  │
│                                                        │
│              [ 取消  ]  [ 保存 ]                       │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**自定义任务与 AI 筛选集成：**

自定义任务的指标字段会自动出现在 AI 筛选器中：

```
┌────────────────────────────────────────────────────────┐
│  AI 筛选                                               │
├────────────────────────────────────────────────────────┤
│                                                        │
│  内置指标:                                             │
│  ☑️ 综合评分 > [  70  ]                               │
│  ☑️ 美学评分 > [  60  ]                               │
│  ☑️ 情感评分 > [  50  ]                               │
│                                                        │
│  自定义指标:                                           │
│  ☑️ 旅行打卡分 > [  75  ]  (自定义任务: 我的旅行打卡)  │
│  ☑️ 地标分 > [  80  ]       (自定义任务: 我的旅行打卡) │
│                                                        │
│  [  应用筛选  ]                                        │
│                                                        │
└────────────────────────────────────────────────────────┘
```

#### 2.3.4 任务调度策略

| 配置项 | 产品要求 |
|--------|----------|
| 并行任务数 | 默认 2，可调 1~4（设置 → 分析设置） |
| 任务优先级 | P0（打分/标签）> P1（人物/地理/视频）> P2（重复检测） |
| 状态控制 | 支持暂停、继续、停止、失败重试 |
| 进度展示 | 侧边栏 + 菜单栏显示进度与已处理数量 |

> 调度器架构、Actor 模型、断点续传实现见 [TECH.md §2.1、§2.2](TECH.md)。

#### 2.3.5 首次启动策略

| 场景 | 产品行为 |
|------|----------|
| 库规模 &lt; 1000 | 直接全量分析 |
| 库规模 ≥ 1000 | 默认先分析当年，完成后询问是否继续历史照片 |
| 后台运行 | 不阻塞用户正常使用 Mac |

#### 2.3.6 断点续传

- 分析进度持久化，应用重启后从中断处继续
- 已完成资源不重复分析（除非用户主动触发重新分析）

> 持久化字段与状态机见 [TECH.md §2.2](TECH.md)。

### 2.4 照片评分系统

#### 2.4.1 双评分体系与评分维度（百分制）

**双评分定义：**

| 评分名称 | 定义 | 可配置性 |
|----------|------|----------|
| **美学评分** | 严格摄影美学分（截图固定 0 分） | ❌ 锁定任务，提示词不可修改 |
| **综合评分** | 基于六维加权计算的总分 | ✅ 权重可调 |

**综合评分默认权重（可自定义修改）：**

| 维度 | 默认权重 | 评分标准 | 是否可配置 |
|------|----------|----------|-----------|
| **美学维度（来源于美学评分）** | 30% | 构图（平衡/层次/主体）、清晰度、色彩（和谐/饱和）、光线（自然/曝光） | ✅（权重可调，分数源不可改） |
| **技术质量** | 30% | 清晰度、噪点、曝光准确、抖动程度 | ✅ |
| **内容价值** | 15% | 是否有意义（活动、旅行、运动、聚会、人物等） | ✅ |
| **情感价值** | 10% | 人物数量、人物亲密程度、人物表情、情感表达、回忆感 | ✅ |
| **稀有性** | 10% | 时间稀有程度、地点稀有程度 | ✅ |
| **独特性** | 5% | 与其他照片的重复度（越独特越高） | ✅ |

> **权重配置入口：** 设置 → 评分权重 → 各维度滑块（0%~100%，总和需等于100%）

**关系约束：**

- 每张照片持有「综合评分」与「美学评分」两种分值；卡片角标默认展示美学评分，可在照片页工具栏的分段控件中一键切换为综合评分（无需进入设置页）
- 综合评分中的美学维度分值必须来自「美学评分（严格）」任务输出，不允许由综合评分任务重算

**权重修改后重新打分流程：**

```
用户修改权重 → 提示："权重已修改，是否对照片/视频重新打分？"
              → [ 重新打分全部 ] [ 仅重新打分的照片/视频 ] [ 暂不重新打分 ]
              → 重新打分后覆盖原结果
```

#### 2.4.2 重复照片降分机制

- 系统自动识别相似/重复照片并分组
- 组内保留质量最高的一张，其余照片综合分显著降低并标记为重复
- 用户可在筛选页通过「重复/相似」芯片筛选

> pHash 算法、降分公式与数据库字段见 [TECH.md §2.4](TECH.md)。

#### 2.4.3 一键筛选删除

**删除流程（三步确认）：**

```
Step 1: 用户设置分数滑块（0-100可调，可手动输入）
Step 2: 点击"预览" → 显示符合条件的所有照片
Step 3: 点击"移至废纸篓" → 弹出二次确认对话框
        ┌────────────────────────────────────────┐
        │  确认删除                               │
        │  ───────────────────────────────────── │
        │  即将删除 1,234 张照片                  │
        │  这些照片将移至系统废纸篓               │
        │                                        │
        │  ☑️ 我确认要删除这些照片               │
        │                                        │
        │        [ 取消  ]  [ 确认删除 ]         │
        └────────────────────────────────────────┘
Step 4: 确认后执行删除（调用 Photos Framework API）
```

**筛选器界面（Demo 已实现核心交互）：**

```
┌────────────────────────────────────────────────────────┐
│  筛选                                                  │
├────────────────────────────────────────────────────────┤
│  维度评分范围（7维）                                     │
│  [美学][综合][技术][内容][情感][稀有][独特] ← 维度切换    │
│  [ 大于 ] [ 小于 ]   ← 比较运算符切换                   │
│  0 [=======●========] 100   [ 60 ]                     │
│  只显示美学评分大于 60 分的照片（默认）                    │
│                                                        │
│  时间范围（天/周/月/年分排 + 特殊选项，见 8.4）          │
│                                                        │
│  设备与来源: [专业设备] [专业软件调整]                   │
│  其他筛选:   [重复/相似] [近一月新增] [已存iCloud]       │
│                                                        │
│  预览 + 移至废纸篓（见 8.3）                             │
└────────────────────────────────────────────────────────┘
```

**评分比较规则：**

| 规则 | 说明 |
|------|------|
| 维度切换 | 可在美学/技术/内容/情感/稀有/独特中任选一个维度，默认美学 |
| 大于 | `selected_dimension_score > 阈值` |
| 小于 | `selected_dimension_score < 阈值` |

- 滑块与数字输入框**双向同步**（0–100）
- 下方提示文案随运算符和维度实时更新，例如「只显示技术质量**小于** 60 分的照片」

### 2.5 标签系统

#### 2.5.1 自动标签分类

| 标签大类 | 示例标签 |
|----------|----------|
| **场景** | 海滩、山景、城市、乡村、夜景、室内、室外、森林、沙漠 |
| **物品** | 食物、动物、植物、汽车、建筑、天空、云朵、海洋、书籍 |
| **人物** | 人像、合影、自拍、背影、孩童、宠物、多人、单人 |
| **活动** | 旅行、聚餐、运动、婚礼、生日、会议、演出、购物 |
| **时间** | 日出、日落、正午、傍晚、夜晚、黄金时刻、蓝调时刻 |
| **天气** | 晴天、多云、雨天、雪天、雾天、雷电 |
| **情绪** | 开心、悲伤、浪漫、宁静、热闹、庄严、神秘 |

#### 2.5.2 自定义标签

- 用户可添加自定义标签规则
- 支持正则表达式匹配
- 支持按时间/地点自动打标签

### 2.6 搜索功能

#### 2.6.1 搜索方式

| 搜索类型 | 示例 |
|----------|------|
| **自然语言** | "去年夏天在海边拍的照片"、"有狗狗的照片" |
| **标签筛选** | 场景:海滩 + 人物:宠物 |
| **分数筛选** | 分数 > 80 |
| **时间筛选** | 2025年春节、2024年夏天 |
| **地点筛选** | 北京、上海、日本 |
| **AI 筛选** | 综合评分>70 AND 情感评分>60 AND 稀有性>50 |
| **组合筛选** | 场景:海滩 AND 分数:>70 AND 时间:2025 |

#### 2.6.2 搜索结果展示

- 网格视图：大图预览
- 列表视图：缩略图 + 关键信息
- 支持结果排序（时间、分数、相关度）

#### 2.6.3 智能搜索中心（产品方案）

**设计目标：**

- 用一句话让用户快速找回记忆，不要求用户理解筛选器语法
- 让搜索成为“入口能力”：既能直接给结果，也能一键跳转到照片页继续操作
- 让状态可见：当前查询词、匹配数量、匹配依据（时间/分数/标签）可反馈

**入口设计：**

| 入口 | 触发方式 | 预期行为 |
|------|----------|----------|
| 顶部导航「搜索」 | 点击 Tab | 进入搜索页，焦点落在输入框 |
| 底部导航「搜索」 | 点击导航项 | 同上 |
| 快捷键 | `⌘K` / `Ctrl+K` | 任意页面直接唤起搜索页并聚焦输入框 |

**交互流程：**

```
进入搜索页 → 输入自然语言 → 实时显示建议/结果数量
          → 选择建议词（可选）→ 结果列表更新
          → 点击结果项「定位」→ 跳回照片页并打开目标照片详情
```

**查询解析优先级（Demo 规则）：**

1. **显式分数条件**（如“综合评分大于80”、“美学评分>85”）
2. **时间语义**（今年/去年/近一月）
3. **标签语义**（旅行/宠物/建筑/美食等）
4. **地点词匹配**（北京/上海/厦门等）

**关键状态与空态：**

- 空输入：展示搜索建议
- 有输入但无结果：展示“无匹配结果”并引导调整关键词
- 有结果：展示匹配数量 + 结果卡片，支持定位回照片页

### 2.7 照片管理

| 功能 | 描述 |
|------|------|
| **移至废纸篓** | 两步确认后，将照片移至系统废纸篓 |
| **恢复照片** | 从废纸篓恢复 |
| **批量操作** | 批量选择、批量删除、批量打标签 |
| **单张详情** | 查看维度分、标签、时间地点、相似组与保留建议 |
| **收藏** | 标记为收藏，保护不被删除建议覆盖 |
| **隐藏分析** | 将某些照片排除在分析队列外 |

### 2.8 分析报告

#### 2.8.1 报告存储

- 格式：**Markdown（.md）**
- 存储位置：**SQLite 数据库**（photos_reports 表）
- 报告内容：分析摘要、统计信息、重要发现

#### 2.8.2 报告管理

```
┌────────────────────────────────────────────────────────┐
│  分析报告                                              │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │ 📄 2026-05-25 首次分析报告      2026-05-25   │   │
│  │    照片 45,678 张 | 视频 3,214 个             │   │
│  │    平均分 68.5 | 废片建议 12,345 张           │   │
│  │    [ 查看 ] [ 删除 ]                         │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │ 📄 2026-05-20 周度报告          2026-05-20   │   │
│  │    照片 1,234 张 | 视频 89 个                 │   │
│  │    [ 查看 ] [ 删除 ]                         │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
│  [+ 生成新报告]                                        │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**查看报告：**
- 在软件内嵌的 Markdown 阅读器中查看
- 支持复制、导出到桌面

### 2.9 交互式 Demo 原型

为便于评审 UI/UX，项目提供可浏览器打开的 HTML 原型：

| 路径 | 说明 |
|------|------|
| `demo/index.html` | 主界面：6 个 Tab + 侧边栏 + 弹窗 |
| `demo/css/style.css` | Liquid Glass 风格样式与 CSS 变量 |
| `demo/js/app.js` | 页面路由、筛选逻辑、配置弹窗等演示逻辑 |

**Demo 已覆盖的能力摘要：** 照片网格（7级 `−`/`+` 缩放，丝滑过渡；工具栏分段控件切换角标评分维度；4选项联动排序）、侧边栏 overlay 模式（磨砂玻璃叠加照片上方，滚动 >10px 自动隐藏，停滚恢复；8个默认标签，更多/收起带图标）、照片详情弹窗（维度分/标签/地点时间/相似组/保留建议/内容描述/照片回忆）、时间线按年浏览、智能搜索（自然语言 + 热门词随机推荐 + 定位）、筛选（7维评分切换 + 大于/小于 + 时间快捷 + 设备/其他芯片）、报告列表、设置（模型/权重/提示词）、双导航与主题切换。

**完整交互清单见第八章。** 正式版实现映射见 [TECH.md §7.2](TECH.md)。

---

## 三、相关技术文档

技术架构、数据库设计、模块实现、性能优化、开发任务与计划已独立成文：

| 文档 | 内容 |
|------|------|
| **[TECH.md](TECH.md)** | 技术栈、SQLite 表结构、TaskScheduler/ScoringEngine 设计、MLX-VLM 集成、性能策略、Phase 1–9 TODO、开发估算、风险应对 |

---

## 四、UI 设计

### 4.1 设计语言：Apple Liquid Glass（液态玻璃）

**macOS Tahoe 最新设计语言特性：**

| 特性 | 说明 |
|------|------|
| **毛玻璃背景** | 使用 `.glass` / `.ultraThinMaterial` 系列背景 |
| **半透明窗口** | 窗口背景半透明，呈现深度感 |
| **柔和边框** | 边框极细（0.5pt），颜色淡雅 |
| **动态光效** | 模拟光线折射效果 |
| **模糊层次** | 背景模糊强度分级（ultraThin/thin/regular/thick） |
| **圆角半径** | 大圆角（16-24pt）营造柔和感 |

**色彩配置（适配 Light/Dark Mode）：**

| 元素 | Light Mode | Dark Mode |
|------|-----------|-----------|
| 窗口背景 | `.ultraThinMaterial` | `.ultraThinMaterial` |
| 侧边栏背景 | `.sidebar` | `.sidebar` |
| 卡片背景 | `.secondaryUltraThinMaterial` | `.secondaryUltraThinMaterial` |
| 主色调 | System Blue | System Blue |
| 强调色 | System Indigo | System Indigo |
| 文字主色 | System Label | System Label |
| 文字次色 | System Secondary Label | System Secondary Label |

### 4.2 窗口结构（与 Demo 对齐）

```
┌──────────────────────────────────────────────────────────────────┐
│ [◀] MantaPhotos  [照片][时间线][搜索][筛选][报告][设置] 🌙 🔔 │
├──────────────┬───────────────────────────────────────────────────┤
│  侧边栏       │  主内容区（当前 Tab 对应页面）                      │
│  (仅照片页)   │                                                   │
│              │                                                   │
│  标签筛选     │                                                   │
│  快捷操作     │                                                   │
│  AI 分析进度  │                                                   │
│              │                                                   │
├──────────────┴───────────────────────────────────────────────────┤
│  [照片] [时间线] [搜索] [筛选] [报告] [设置] ← 底部液态玻璃导航（6项） │
└──────────────────────────────────────────────────────────────────┘
```

**导航规则（Demo 已实现）：**

| 规则 | 说明 |
|------|------|
| **双导航同步** | 顶部 Tab 与底部导航项一一对应，切换时同步高亮 |
| **侧边栏显隐** | 仅在「照片」页显示侧边栏及折叠按钮；其他页面自动隐藏 |
| **侧边栏折叠** | 左上角按钮可折叠/展开，图标在 `chevron-left` / `chevron-right` 间切换 |

> **说明：** 搜索页与照片详情弹窗已在 Demo 中落地（见 8.12、8.13）；自定义任务编辑器仍为后续版本重点。

### 4.3 主要视图

| 视图 | Demo 状态 | 说明 |
|------|-----------|------|
| **照片页** | ✅ 已实现 | 网格展示（单分角标，工具栏分段控件即时切换美学/综合）、排序、AI 分析入口、多选 |
| **时间线页** | ✅ 已实现 | 按年月事件轴、年份切换、旅行角标、地图视图（占位） |
| **筛选页** | ✅ 已实现 | 7维评分切换筛选（大于/小于，默认美学）、时间快捷、设备/其他芯片筛选 |
| **报告页** | ✅ 已实现 | 报告列表、生成/查看/删除（演示数据） |
| **设置页** | ✅ 已实现 | 模型列表、导入、并行数、首年策略、权重/提示词弹窗 |
| **搜索页** | ✅ 已实现 | 自然语言检索、建议词、快捷键唤起、结果定位到照片页（见 8.12） |
| **照片详情弹窗** | ✅ 已实现 | 单张详情、双评分、相似组、保留建议、内容描述与照片回忆（见 8.13） |
| **自定义任务编辑器** | 📋 规划中 | 见 2.3.3 |

### 4.4 设计规范

| 设计元素 | 规范 |
|----------|------|
| **设计风格** | Apple Human Interface Guidelines + Liquid Glass |
| **颜色方案** | 支持 Light/Dark Mode，自动跟随系统 |
| **字体** | SF Pro（系统默认） |
| **图标** | SF Symbols |
| **圆角** | 卡片 16pt，窗口 12pt，按钮 8pt |
| **间距** | 8pt 网格系统 |
| **毛玻璃** | `.ultraThinMaterial` / `.regularMaterial` |
| **动作层级** | 每页保留一个主动作，次动作收敛到工具栏/弹窗 |
| **关键流程** | 高风险操作遵循「预览 → 确认 → 执行」闭环 |

### 4.5 交互哲学（产品评审结论）

- **聚焦而非堆叠：** 每个页面只解决一个核心问题（照片浏览、时间回忆、筛选决策、报告复盘、设置调优）。
- **可见的状态：** 让用户始终知道系统“现在处于什么状态”（当前筛选、当前模型、当前策略、当前选中数量）。
- **先预览再破坏：** 删除等不可逆动作必须先展示结果规模，再进入确认。
- **默认即最佳：** 默认模型、默认权重、默认年份应直接可用，减少首次学习成本。

---

## 五、文档目录结构

```
MantaPhotos/
├── SPEC.md                    # 产品需求与交互规格（本文档）
├── TECH.md                    # 技术设计与实现规格
├── README.md                  # 项目介绍
├── demo/                      # 可交互 HTML 原型（见 SPEC 第 2.9、第八章）
├── docs/                      # 扩展设计文档（待创建）
├── src/                       # 项目代码（待创建）
└── assets/                    # 设计资源（待创建）
```

---

## 六、评审要点

评审时确认以下内容：

1. ✅ 产品功能是否完整覆盖需求？
2. ✅ 评分权重与提示词配置是否符合预期？
3. ✅ 自定义分析任务机制是否满足需求？
4. ✅ UI 设计（液态玻璃）与 Demo 交互是否符合预期？

技术评审见 [TECH.md](TECH.md)：选型、数据库、开发计划与风险。

---

## 七、附录

> HuggingFace 模型下载地址见 [TECH.md §7.1](TECH.md)。

### 7.1 默认评分权重（可直接复制）

| 维度 | 字段名 | 默认权重 |
|------|--------|----------|
| 美学评分 | aesthetics_score | 0.30 |
| 技术质量 | technical_score | 0.30 |
| 内容价值 | content_score | 0.15 |
| 情感价值 | emotion_score | 0.10 |
| 稀有性 | rarity_score | 0.10 |
| 独特性 | uniqueness_score | 0.05 |

### 7.2 内置提示词（Demo 默认任务）

Demo 设置页「AI 提示词配置」中内置 7 项任务（其中 1 项锁定不可编辑）：

| 任务 ID | 名称 | 默认提示词摘要 |
|---------|------|----------------|
| `score` | 综合评分 | 汇总多维度分值，计算 final_score |
| `aestheticsStrict` | 美学评分（严格） | 摄影美学严格评分；截图固定 0 分；锁定不可编辑 |
| `tag` | 内容标签 | 场景/物品/人物/活动标签数组 |
| `person` | 人物识别 | 人物数量、表情、自拍特征 |
| `location` | 地理位置 | 地理位置、是否为旅行照 |
| `description` | 照片内容描述（≤300字） | 拍摄内容描述 + 科普信息 |
| `diary` | 照片回忆（≤200字） | 同行人/时间/城市/行程/心情叙事 |

### 7.3 文档版本记录

> 完整版本历史与里程碑说明见文档首部**变更日志**章节。

| 版本 | 说明 |
|------|------|
| v0.0.1 ~ v0.0.6 | 各页面规格逐步成稿（搜索/筛选/时间线/报告/设置/详情） |
| v0.0.7 | 与 Demo 实现对齐，评审版 |
| v0.0.8 | 角标切换移至照片页工具栏分段控件 |
| v0.0.9 | 工具栏重排、7级缩放、联动排序、侧边栏边缘标签 |
| **v0.0.10** | **🎯 小里程碑**：缩放修正、侧边栏 overlay、滚动隐藏、标签云 8 个 |

---

*文档由 Eliza (AI 助手) 生成，待 Venk 评审后开始开发。*

---

## 八、界面交互规范（Demo 实现版）

> 本章根据 [`demo/index.html`](demo/index.html)、[`demo/css/style.css`](demo/css/style.css)、[`demo/js/app.js`](demo/js/app.js) **反向整理**，描述当前可交互原型中已实现的产品行为。  
> 标注 **📋 规划** 的条目为规格已有、Demo 尚未实现。

### 8.0 应用壳层与全局交互

| 模块 | 实现状态 | 说明 |
|------|----------|------|
| 顶部栏 | ✅ | Logo、6 个 Tab、主题切换、通知（占位） |
| 底部导航 | ✅ | 6 项液态玻璃导航，与顶部 Tab 同步 |
| 侧边栏折叠 | ✅ | 仅照片页显示；`sidebarToggle` 折叠/展开 |
| Toast 反馈 | ✅ | 右上角堆叠提示，约 3 秒自动消失 |
| Esc 关闭弹窗 | ✅ | 关闭删除确认、权重配置、提示词配置、照片详情 |
| 主题 | ✅ | `data-theme="dark"` 切换浅色/深色，月亮/太阳图标 |

**视觉规范（CSS 变量）：**

- 主背景渐变 + 毛玻璃卡片（`--glass-bg`、`backdrop-filter: blur`）
- 圆角：`--radius-lg` 16px / `--radius-md` 12px / `--radius-sm` 8px
- 强调色：蓝紫渐变（`--accent-gradient-1` / `--accent-gradient-2`）

---

### 8.1 照片页

**页面结构：**

```
┌─ 照片 ─────────────────── 48 张照片 ──────────────────────────┐
│  [− +]  [📷 美学 | 📈 综合]  [排序▼]  [▶ AI 分析]              │
├──────┬─────────────────────────────────────────────────────────┤
│ 侧←→ │  [缩略图+单分角标(默认美学)] [缩略图] ...                 │
│ 边   │                                                         │
│ 栏   │                                                         │
└──────┴─────────────────────────────────────────────────────────┘
```

**工具栏控件（从左到右）：**

| 控件 | 类型 | 说明 |
|------|------|------|
| `[− +]` 缩放 | 两个图标按钮 | 调节每行列数（1/2/4/8/16/32），过渡丝滑 |
| `[📷 美学 \| 📈 综合]` | 分段控件 | 切换角标评分维度，同步影响"按分排序"维度 |
| 排序下拉 | Select | 4 选项：时间新→旧 / 旧→新 / 当前评分高→低 / 低→高 |
| AI 分析 | 主按钮 | 启动分析 |

**角标评分分段控件（交互设计决策）：**

| 方案 | 说明 | 结论 |
|------|------|------|
| 单开关（Toggle） | 隐藏另一个状态，用户需记忆"当前是什么" | ❌ |
| 独立按钮（两个） | 需要额外标签说明语境，占用较多空间 | ❌ |
| **分段控件（Segmented Control）** | 两个选项同时可见，激活态渐变高亮，零认知负担，一触即达 | ✅ 采用 |

**照片网格尺寸调节（7 级）：**

| 级别 | 每行列数 | 适用场景 |
|------|----------|----------|
| 1 | 2 | 横向大图比对 |
| 2 | 4 | 宽松网格浏览 |
| 3 | 6 | 标准网格 |
| 4（默认）| 8 | 日常快速浏览 |
| 5 | 12 | 快速扫描 |
| 6 | 16 | 批量筛选 |
| 7 | 32 | 全量缩览 |

- `−` = 缩小（增加列数），`+` = 放大（减少列数）；到达边界时自动禁用（透明度 28%）
- 切换时 `width` 属性 `0.22s cubic-bezier` 平滑过渡，圆角随列数同步缩放（2列 14px → 32列 2px）

**排序联动规则：**

- 排序选项「按 [X] 评分排序」中的 X 随分段控件动态更新（美学 / 综合）
- 当按分排序时，切换分段控件 → 排序立即以新维度重新排列，并提示"排序已同步"

**侧边栏折叠（Jobs 视角设计）：**

> 控件应当在它所控制的事物旁边，而不是在远处的工具栏。

| 状态 | 折叠标签位置 | 图标 | 说明 |
|------|-------------|------|------|
| 侧边栏**展开** | 贴合侧边栏右边缘（left: 260px） | `‹` | 半透明胶囊，hover 加深 |
| 侧边栏**折叠** | 屏幕左侧边缘（left: 20px） | `›` | 平滑滑至左边缘，随时可点击展开 |

标签本身 `transition: left 0.32s cubic-bezier(0.4,0,0.2,1)` 跟随侧边栏滑动，体感与侧边栏同步移动。主内容区同步 `padding-left` 过渡，照片网格不被侧边栏遮挡。

| 功能 | 实现状态 | 交互说明 |
|------|----------|----------|
| 照片网格 | ✅ | Flexbox 布局，每张显示单一分数角标（默认美学评分） |
| 角标分数切换 | ✅ | 工具栏分段控件（美学 / 综合），即时切换，无需进设置页 |
| 网格尺寸调节 | ✅ | `−`/`+` 按钮，6级，width 丝滑过渡 |
| 评分角标配色 | ✅ | 当前角标分值采用：≥80 绿色 / ≥60 橙色 / <60 红色 |
| 照片计数 | ✅ | 标题旁显示「N 张照片」 |
| 排序 | ✅ | 4选项：时间新→旧 / 旧→新 / 当前评分高→低 / 低→高，评分维度跟随分段控件 |
| 侧边栏折叠标签 | ✅ | 右边缘胶囊标签，展开/折叠随侧边栏滑动，主内容区 padding 同步过渡 |
| AI 分析 | ✅ | 主按钮启动分析并同步侧边栏状态 |
| 查看详情 | ✅ | 点击照片卡片打开详情弹窗 |
| 多选勾选 | ✅ | 点击卡片右上角勾选框切换选中态，显示底部多选工具栏 |
| 多选工具栏 | ✅ | 已选数量；移动、收藏、删除 |
| 删除确认 | ✅ | 模态框显示删除数量，确认后真正移除照片 |

**侧边栏（仅照片页）：**

| 区块 | 内容 |
|------|------|
| **标签筛选** | 搜索框 + 标签云（默认标签）+「更多」展开扩展标签，点击即应用筛选 |
| **快捷操作** | 我的收藏、废片篓、高分照片，点击即切换预设筛选 |
| **AI 分析进度** | 状态图标 + 状态文案（分析中/已暂停/已停止）+ 进度条 + 已处理/总数 + 暂停/继续/停止 |

**筛选协同规则：**

- 点击「全部」标签会清空快捷筛选状态，回到默认浏览集合
- 快捷筛选与筛选页高级条件解耦：先选集合，再做高级筛选预览

---

### 8.2 时间线页

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 年份选择 | ✅ | 下拉仅展示有照片的年份，默认优先当前年份（无数据则回退到最近有数据年份） |
| 年份切换 | ✅ | 左右箭头切换年份并刷新列表 |
| 事件轴 | ✅ | 按月聚合：日期、标题、横向照片条 |
| 旅行角标 | ✅ | `isTravel: true` 的事件显示「✈ 旅行」药丸标签 |
| 空状态 | ✅ | 该年无数据时提示「N 年暂无记录」 |
| 地图视图 | 📋 | 按钮占位，Toast「地图视图开发中」 |

**时间线数据结构（Demo）：**

```typescript
interface TimelineEvent {
  date: string;       // 如 "2025年8月"
  title: string;      // 如 "三亚之旅"
  photos: string[];   // 缩略图 URL 列表
  isTravel: boolean;  // 是否旅行事件
}
```

**默认演示数据：**

| 年月 | 标题 | 旅行 |
|------|------|------|
| 2025年8月 | 三亚之旅 | ✅ |
| 2025年7月 | 张家界登山 | ✅ |
| 2025年6月 | 北京美食周 | — |
| 2025年5月 | 宠物日常 | — |
| 2024年12月 | 圣诞聚会 | — |
| 2024年10月 | 厦门秋游 | ✅ |

---

### 8.3 筛选页

**筛选维度（当前 Demo）：**

| 维度 | 实现状态 | 说明 |
|------|----------|------|
| **维度评分范围（7维）** | ✅ | 见下表 |
| **时间范围** | ✅ | 见 8.4 |
| **设备与来源** | ✅ | 专业设备、专业软件调整（多选芯片） |
| **其他筛选** | ✅ | 重复/相似、近一月新增、已存 iCloud |
| **标签筛选** | 📋 | 规格含标签芯片；当前 Demo 未在筛选页展示 |
| **预览 + 废纸篓** | ✅ | 预览显示结果数量；删除走确认弹窗并执行 |

**维度评分范围交互：**

| 控件 | 行为 |
|------|------|
| 维度切换 | 7 维单选：美学/综合/技术/内容/情感/稀有/独特，默认**美学** |
| 比较运算符 | 「大于」「小于」二选一，默认**大于** |
| 滑块 | 0–100，`oninput` 实时同步 |
| 数字框 | 手动输入，失焦/变更时同步滑块 |
| 提示文案 | 「只显示{当前维度}{大于\|小于} **N** 分的照片」 |

**筛选逻辑（`matchesScoreFilter`）：**

```
大于: getMetricScore(photo, selectedMetric) > threshold
小于: getMetricScore(photo, selectedMetric) < threshold
```

**应用规则（当前 Demo）：**

- 调整筛选条件时，先更新「预览结果」数量
- 点击「预览结果」后才将筛选正式应用到照片页
- 点击「移至废纸篓」会按当前预览结果进入删除确认

---

### 8.4 时间范围快捷筛选

- 按**天 / 周 / 月 / 年**分四排，每排左侧为日历类图标
- 点击芯片**单选**高亮（`active` 渐变样式）
- 选中后在底部 **已选范围条** 展示起止日期或特殊文案，支持 **× 清除**
- **特殊筛选**（紫色星标行）：最近一次旅行、法定节假日
- **全部时间** 单独一行，虚线边框样式；选中后隐藏已选范围条

| 分类 | 选项 |
|------|------|
| 天 | 今天、昨天、近3天 |
| 周 | 本周、近一周、上周、近两周 |
| 月 | 本月、近一月、上月、近3月、近6月 |
| 年 | 本年、去年、近两年 |
| 特殊 | 最近一次旅行（演示：2025-08-01 至 2025-08-15）、法定节假日（演示：春节·国庆·劳动节） |
| 全部 | 全部时间 |

**日期计算：** 以本地「今天」为基准动态计算；法定节假日为演示固定文案。

---

### 8.5 报告页

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 报告列表 | ✅ | 卡片：标题、日期、照片数、平均分 |
| 生成新报告 | ✅ UI | Toast 模拟生成流程 |
| 查看 | ✅ UI | Toast 打开报告 |
| 删除 | ✅ UI | Toast 删除单条 |

**报告类型（演示数据）：**

| 类型 | 示例标题 |
|------|----------|
| `initial` | 首次分析报告 |
| `weekly` | 周度分析报告 |
| `manual` | 旅行照片专项报告 |

**规划：** 内嵌 Markdown 阅读器、导出桌面（见 2.8.2）

---

### 8.6 设置页

#### 8.6.1 AI 模型

| 模型 | 标签 | 操作态（Demo） |
|------|------|----------------|
| Qwen2.5-VL-3B | 快速 | 可切换为使用中 |
| Qwen2.5-VL-7B | 均衡 | 可切换为使用中 |
| Qwen3-VL-4B | ⭐ 默认 | 初始使用中 |
| Qwen3-VL-8B | — | 可切换为使用中 |
| Qwen3-VL-30B-A3B | 旗舰 | 可切换为使用中 |

- **手动导入模型**：入口「从本地导入 MLX 模型文件」（演示 Toast）
- 存储路径见 2.2.1

#### 8.6.2 分析设置

| 配置项 | Demo 表现 |
|--------|-----------|
| 并行任务数 | 下拉 1–4，默认 2 |
| 首次分析策略 | 点击循环切换：仅今年 → 近三年 → 全量 |
| 评分权重配置 | 打开模态框（见 8.7） |
| AI 提示词配置 | 打开模态框（见 8.8） |

> **注：** 「照片角标分数显示」已从设置页移至**照片页工具栏分段控件**，见 8.1。

---

### 8.7 评分权重配置弹窗

| 功能 | 说明 |
|------|------|
| 六维滑块 | 美学 30%、技术 30%、内容 15%、情感 10%、稀有 10%、独特 5% |
| 分值来源约束 | 美学维度分值固定来自「美学评分（严格）」任务输出（截图=0） |
| 实时总和 | 显示权重总和，≠100% 时警告并禁止保存 |
| 恢复默认 | 一键恢复上述默认比例 |
| 保存 | 总和=100% 时可保存，Toast 提示 |

与 **2.4.1**、**7.1** 字段一致；`weightConfig` 数组驱动 UI 渲染。

---

### 8.8 AI 提示词配置弹窗

| 功能 | 说明 |
|------|------|
| 任务列表 | 7 个内置任务；其中「美学评分（严格）」带锁定标识 |
| 展开编辑 | 非锁定任务可「编辑」展开 `textarea` |
| 锁定规则 | 美学评分任务只读展示，提示词不可编辑、不可自定义 |
| 保存 / 取消 | 保存仅回写可编辑任务提示词并关闭弹窗；取消仅关闭 |

默认任务与文案见 **7.2**。

---

### 8.9 多选与删除（Demo 简化版）

**多选工具栏（选中 ≥1 张时自底部浮出）：**

| 按钮 | 功能 |
|------|------|
| 移动 | 演示 Toast |
| 收藏 | 演示 Toast |
| 删除 | 打开删除确认模态框 |

**删除确认（当前 Demo）：**

- 文案：「确定要删除选中的 N 张照片吗？此操作不可撤销。」
- 取消 / 确认删除
- 删除数量来自当前选择或筛选预览结果
- 确认后会从照片集合中移除，并刷新计数与网格

**规划增强（见 8.10）：** 照片/视频分计、强制勾选确认

---

### 8.10 规划中的交互（规格有、Demo 未实现）

| 模块 | 参考章节 |
|------|----------|
| 多选：全选、排除分析 | 2.7 |
| 删除确认：勾选框 + 视频统计 | 2.4.3 |
| 自定义分析任务编辑器 | 2.3.3 |
| iCloud 一键下载区块 | 2.2、备份版设置页 |
| 报告 Markdown 内嵌阅读器 | 2.8.2 |

---

### 8.11 底部导航与主题

**底部导航（6 项）：**

| 图标 | 页面 ID | 名称 |
|------|---------|------|
| `fa-images` | `photos` | 照片 |
| `fa-clock` | `timeline` | 时间线 |
| `fa-search` | `search` | 搜索 |
| `fa-filter` | `filter` | 筛选 |
| `fa-file-alt` | `reports` | 报告 |
| `fa-cog` | `settings` | 设置 |

**设计参数：** 高度约 64px、圆角 32px、`backdrop-filter: blur(30px)`，激活项高亮。

**主题：** 顶栏右侧按钮切换 Light/Dark，CSS 变量全局生效。

---

### 8.12 搜索页（已实现）

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 搜索输入框 | ✅ | 支持自然语言输入，实时解析并更新结果 |
| 建议词面板 | ✅ | 热门搜索（每次随机展示 3 项）+ 快捷筛选，点击后自动执行搜索 |
| 清空动作 | ✅ | 一键重置输入、结果和提示状态 |
| 结果区 | ✅ | 显示匹配数量与结果卡片（评分/地点/日期/标签） |
| 定位动作 | ✅ | 点击「定位」跳回照片页并打开目标照片详情 |
| 快捷键唤起 | ✅ | `⌘K` / `Ctrl+K` 从任意页面进入搜索并聚焦输入框 |

**查询解析规则（Demo）：**

- 分数：识别“综合评分/美学评分 + 大于/小于/高于/低于/>/<”
- 时间：识别“今年/去年/20xx年/近一月”
- 语义标签：旅行、风景、人物、人像、建筑、美食、宠物、夜景
- 地点：北京、上海、厦门、南京、杭州、三亚、成都、西安、广州、深圳

---

### 8.13 照片详情弹窗（已实现）

| 功能 | 实现状态 | 说明 |
|------|----------|------|
| 打开方式 | ✅ | 照片页点击卡片打开；搜索结果「定位」后自动打开 |
| 双评分展示 | ✅ | 详情标题显示「综合评分 + 美学评分」 |
| 维度分展示 | ✅ | 美学/技术/内容/情感/稀有/独特 6 维条形分值 |
| 截图判定规则 | ✅ | 判定为截图时，美学评分强制为 0，并在详情中标注 |
| 基础信息 | ✅ | 显示拍摄时间、地点、标签 |
| 内容描述 | ✅ | 300 字以内描述拍摄内容并补充科普信息 |
| 照片回忆 | ✅ | 200 字以内生成行程叙事与心情分析 |
| 相似组信息 | ✅ | 显示重复组 ID 与组内质量排名；非重复则显示独立保留提示 |
| 保留建议 | ✅ | 按分数与重复状态输出「保留/复核/清理」建议 |
| 快捷动作 | ✅ | 收藏/取消收藏、删除此照片（复用删除确认流程） |

**交互闭环：**

1. 点击照片 → 打开详情弹窗  
2. 查看维度分 + 相似组 + 保留建议  
3. 决策动作：收藏或删除  
4. 删除走统一确认模态框，执行后刷新照片集合

---

### 8.14 Demo 与正式版映射

见 [TECH.md §7.2](TECH.md)。

---

*本章节随 Demo 迭代更新；以 `demo/` 目录源码为准。*
