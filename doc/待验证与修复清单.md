# MantaPhotos 待编译验证与修复清单（交接 Claude Code）

本文档列出最近几轮在**无 macOS 工具链环境**下完成、尚未真实编译的改动，供 Claude Code 在 Mac 上 **编译 → 运行 → 测试新功能 → 修复问题**。

所有改动已提交到本地 `master`，**尚未推送**。改动均通过了静态校验（括号/引号配平、符号与调用点交叉核对），但 Swift 6 严格并发、GRDB、AVFoundation 等只能在 Xcode/SwiftBuild 下最终确认。

---

## 0. 环境与构建

- 平台：macOS 26（`Package.swift` 指定 `.macOS("26.0")`），Swift 6.0（**strict concurrency**），依赖 GRDB 7.11+。
- 工程有两套：SwiftPM（`MantaPhotos/Package.swift`）+ XcodeGen（`MantaPhotos/project.yml` 生成 `MantaPhotos.xcodeproj`）。
- **重要**：本轮新增了多个 `.swift` 文件。SwiftPM 按目录 glob 会自动包含；但 `MantaPhotos.xcodeproj` 若不重生成可能漏掉新文件 → **先 `cd MantaPhotos && xcodegen generate` 再用 Xcode 打开**。

构建 / 测试命令：

```bash
cd MantaPhotos
# 1) 命令行编译核心 + 可执行
swift build
# 2) 跑单元测试（Swift Testing，含 migration 测试）
swift test
# 3) Xcode（GUI 运行 / 沙箱 entitlements 生效）
xcodegen generate
open MantaPhotos.xcodeproj   # 选 MantaPhotosMac scheme 运行
```

> 注：沙箱权限（照片库、用户选择文件、安全作用域书签）只有在 Xcode 签名运行的 app bundle 下才完整生效；`swift run` 的裸可执行不一定带 entitlements。本地目录源的真机验证请用 Xcode 运行。

新增文件清单（需确认都进了编译目标）：

- `Sources/MantaPhotosMac/Domain/PhotoSource.swift`
- `Sources/MantaPhotosMac/Infrastructure/Database/PhotoSourceRepository.swift`
- `Sources/MantaPhotosMac/Infrastructure/Sources/LocalMediaSupport.swift`
- `Sources/MantaPhotosMac/Infrastructure/Sources/LocalDirectoryImporter.swift`

---

## 1. 本轮改动总览（按提交）

| commit | 内容 |
| --- | --- |
| `5037ac8` | 液态玻璃收敛 + 底部导航坞 + 查看器快捷键/手势 + 评分与滚动性能优化 + 缩略图畸变修复 |
| `2ac1bf9` | 顶部标签栏固定高度 48pt |
| `9dea42b` | material-first 收口（前瞻适配 macOS 27 半透明度滑块） |
| `f3e2f91` | **多照片源重构 M1–M5（最大改动）** |
| `ebbfaa9` | 迁移测试更新为 5 + 校验 photo_sources |

最高风险、最需要重点验证的是 `f3e2f91`（多照片源）。

---

## 2. 优先级最高：编译风险热点（先看这些）

按「最可能编译报错」排序，逐个确认：

### 2.1 Swift 6 严格并发

- `Infrastructure/Sources/LocalMediaSupport.swift` → `LocalThumbnailProvider.requestThumbnail`
  - `queue.async { [weak self] in ... Task { @MainActor in completion(image) } }`：依赖 region-based isolation 把本地 `NSImage` 送进主线程。若报「non-Sendable captured by sending closure」，改为：在后台先得到 `image`，用 `MainActor.assumeIsolated` 或把 `image` 包进一个 `@unchecked Sendable` 盒子再回主线程。
  - 确认捕获的 `completion`（`@MainActor @escaping`）被视为 Sendable。
- `Infrastructure/Photos/PhotoThumbnailProvider.swift` → `requestSystemThumbnail` 里 `Task { @MainActor in completion(image) }` 同上。
- `App/PhotoLibraryViewModel.swift` → `runLocalImport` 的 `Task.detached(priority: .utility) { try await importer.importAll() }`：确认 `LocalDirectoryImporter` 的 `Sendable` 成立（其成员 `PhotoAssetRepository`/`String`/`URL` 均 Sendable）。
- `Domain/AnalysisScheduler.swift` → `score(...)` 新增的本地分支在 actor 外的 TaskGroup 子任务里调用 `PhotoSourceRegistry.shared` 与 `LocalMediaProvider()`：确认无 actor 隔离报错。
- `Presentation/Photos/PhotoViewerView.swift` → `@State private var thumbnailToken: ThumbnailRequestToken?`：`ThumbnailRequestToken` 是 class，放 `@State` 仅作存储，应可；若 SwiftUI 抱怨，改为普通属性或包一层。

### 2.2 GRDB

- `Infrastructure/Database/DatabaseMigrator.swift` → `multiPhotoSourceSQL`：一条 `execute(sql:)` 内含多条 `ALTER TABLE ... ADD COLUMN` + `CREATE TABLE/INDEX` + `UPDATE`。确认 GRDB 逐条执行成功；**尤其在已有 v1–v4 的旧库上升级**（不是只测全新库）。
- 列读取：`SearchRepository.map` / `AnalysisRepository.pendingVisionTasks` / `PhotoSourceRepository.map` 用了 `row["source_id"] as String?`、`let x: Int64? = row["file_size"]`、`row["root_bookmark"]`(Data?) 等。确认 GRDB 的泛型下标推断成立。
- `PhotoAssetRepository.upsert`：`values` 数组里有一个裸 `nil`（file_bookmark）和 `Int64?`(file_size)。确认 `StatementArguments(values)` 接受。
- `PhotoSourceRepository.upsert`：`arguments: [...]` 数组字面量含 `Data?`/`String?`，确认 ExpressibleByArrayLiteral 推断成 `StatementArguments`。

### 2.3 AVFoundation / ImageIO

- `LocalMediaSupport.swift` → `AVAssetImageGenerator.copyCGImage(at:actualTime:)` 在 macOS 13+ 标记弃用。**预期只是 warning**；若在 macOS 26 变成 error，改用异步 `generator.image(at:)`（需把 `videoPosterFrame` 改 async，并相应调整 `makeThumbnail` / 评分取帧链路）。
- `imageThumbnail` 的 `kCGImageSourceThumbnailMaxPixelSize: max(64, maxPixel)`：确认 `CFDictionary` 值装箱无误。

### 2.4 仅 warning（可暂不处理）

- `Presentation/Photos/PhotoGridView.swift` 顶部 `import Photos` 现已不直接使用 → 未使用导入 warning。
- `LocalMediaSupport.swift` 的 `import UniformTypeIdentifiers` 实际未用 → 可删。

---

## 3. 多照片源功能验证（M1–M5）

### 3.1 回归：系统图库不能坏

先确认现有系统图库全链路正常（这是底线）：

- 首次启动授权、分阶段导入、网格滚动/预热/触底加载。
- 查看器：图片/视频/Live Photo、左右切换、收藏(`.`)、信息(`⌘I`)、删除(`⌘⌫`)、废片篓(`T`)。
- 评分：评分进行中 **不再逐批刷新**（整轮结束刷新一次），进度条仍实时。
- Find(`⌘K`) 筛选实时更新。
- 升级既有库（v4→v5）后，老照片仍在、评分仍在。

### 3.2 新功能：本地目录源

在「设置 → 照片源」：

1. **添加本地文件夹**：点「添加本地文件夹」→ NSOpenPanel 选目录 → 后台扫描导入 → 网格出现本地照片。
2. **缩略图**：本地图片缩略图正常、无拉伸畸变；本地**视频**显示首帧海报。
3. **查看器**：打开本地图片可缩放/平移/双击/滑动；打开本地**视频**能播放（AVURLAsset）。
4. **评分**：对本地图片评分出分；本地视频评分用抽帧（确认不崩）。
5. **收藏**：本地照片收藏只更新 App 内状态（不写系统）。
6. **删除**：本地照片删除 → 进入**系统废纸篓**（`FileManager.trashItem`），不是硬删除。
7. **重扫**：目录里新增/删除文件后点「重新扫描」能同步。
8. **移除源**：移除后该源照片从库消失（级联删除），系统源不可移除。
9. **混合**：系统 + 本地照片在同一网格共存、互不串味（缩略图/查看器/评分按源路由）。
10. **重启**：重启 app 后本地源仍可访问（安全作用域书签解析成功）。

### 3.3 数据库

- 全新库：跑完迁移有 5 条 `schema_migrations`，存在 `photo_sources` 表与 `photo_assets.source_id` 列，且有一行 `system_photos`（`swift test` 已覆盖）。
- 旧库升级：v4 库升级到 v5 不报错、存量行 `source_id='system_photos'`、`source_asset_key=local_identifier` 回填正确。

---

## 4. 其它本轮 UI/性能改动的验证

- **底部液态玻璃导航坞**：四分区快速跳转 + 右侧独立 🔍；点击命中正常、不挡网格最后一行；Find 展开时淡出。
- **查看器快捷键**：`←/→`、`空格`、`.`、`⌘I`、`⌘⌫`、`T`、`Esc`；菜单栏 `⌘+`/`⌘-` 改网格缩放。
- **查看器手势**：捏合缩放(1–6×)、放大后拖拽平移、双击适配↔2.5×、未放大左右滑动切换；切换照片复位缩放。
- **缩略图**：`resizeAspectFill`，非方形照片无畸变（若想要不裁剪可改 `resizeAspect`）。
- **顶部标签栏**：固定 48pt，各分区/有无照片高度一致。
- **material-first**：发丝描边 `Color.primary.opacity`、选中态系统强调色、无自绘渐变；macOS 27 半透明度滑块应自动跟随。
- **性能**：评分时滚动跟手；触底加载增量页 800；滚动预热已节流。

---

## 5. 已知保守取舍（**非缺陷**，按需再做）

- **FSEvents 实时监听未做**：本地源用「手动重扫」。后续可加 `FSEventStream` 自动增量同步。
- **跨源去重为基础版**：`content_hash` 列已就绪但导入未逐文件计算（大库性能）；唯一性靠 `(source_id, source_asset_key)`。
- **本地 Live Photo 不配对**：本地资产仅 image/video（`.heic+.mov` 配对、Google Motion 未做）。
- **本地视频精确尺寸/时长**导入阶段从略（需异步 `AVAsset.load`）；不影响网格与评分。
- **移除源未显式 `stopAccessingSecurityScopedResource`**：依赖 app 退出释放；如需严谨可在 registry 保存 URL 并在 unregister 时停。
- **本地截图识别**：本地资产无 `PHAssetMediaSubtype`，截图强制 0 分规则对本地不生效（可后续按文件名/EXIF 兜底）。

---

## 6. 给 Claude Code 的建议执行顺序

1. `cd MantaPhotos && swift build`，逐个修编译错误（优先看第 2 节热点）。
2. `swift test`，确认 `DatabaseMigratorTests`（迁移=5、photo_sources）与 `SearchFilterStateTests` 通过。
3. `xcodegen generate && open` Xcode，运行 app：
   - 先验证系统图库回归（第 3.1）。
   - 再验证本地目录源全流程（第 3.2）。
4. 修复发现的问题；对第 5 节的取舍项，除非需要，否则不必动。
5. 全绿后 `git push origin master`（沙箱无凭据未推）。

---

## 7. 相关设计文档

- 架构与逐文件改造点 / 实现记录：`doc/照片源方案重构清单.md`
- 液态玻璃与 macOS 27 适配：`doc/LiquidGlass重构清单.md`、`doc/Mac应用实现方案.md`（5.6 / 6.5 / 6.6 / 6.7 / 3.8.2）
- macOS 最终设计：`SPEC.md` 第九章

---

## 8. 最新一轮：手势 / 网格档位 / 年月导航 / 玻璃 / 本地化（**未提交，在工作区**）

> 这一轮改动与第 1–7 节不同：**尚未 `git commit`**，仅在工作区。请 review 后再决定提交。
> 涉及文件：`PhotoViewerView.swift`、`PhotoGridView.swift`、`PhotosPageView.swift`、`AppState.swift`(GridLevel)、`NavigationState.swift`、`AppSettingsRepository.swift`、`DatabaseMigrator.swift`、`AppLocalizer.swift`、`SettingsPageView.swift`、`AppShellView.swift`、`FindOverlayView.swift`、`SPEC.md`。

### 8.1 编译风险热点（先看）

- **`.glassEffect(.regular, in:)`**（侧栏 / 导航坞 / Find 浮层 / 工具条段控）：macOS 26 API。若签名不符或需 `GlassEffectContainer`，按编译器提示调整；不可用时回退 `.background(.regularMaterial, in:)`。
- **`ZoomableImageView` / `SwipeAwareScrollView`**（`PhotoViewerView.swift`）：`NSScrollView` + `NSImageView` documentView 的填充/磁化；确认 `imageView.autoresizingMask=[.width,.height]` 在 magnify 时表现正常；`setMagnification(_:centeredAt:)`、`scrollWheel` 的 `event.phase/momentumPhase` 行为。
- **年/月导航联动**：`PhotoGridView` 新增 `onSectionsChange/onVisibleDateChange/scrollToIndex/onScrollHandled`；`updateNSViewController` 里 `DispatchQueue.main.async { onScrollHandled() }` 复位 `@State`，确认不触发「Modifying state during view update」警告/循环。
- **GridLevel 改 7 档**：旧持久化值（如 4/8）不匹配新 case → 回退 `.default`(5)，确认无崩溃；migration 默认值已改 '5'。

### 8.2 手势对照表（对齐 macOS 系统照片，逐条验收）

**查看器（单张静态图）**

| 手势 | 系统照片 | 本应用（本轮后） | 验收 |
| --- | --- | --- | --- |
| 触控板捏合缩放 | ✅ | ✅ NSScrollView 原生 1–6× | 捏合平滑缩放 |
| 放大后双指拖拽/滚动平移 | ✅ | ✅ 原生 | 放大后双指移动可平移内容 |
| 双击缩放切换 | ✅ | ✅ 适配↔2.5×，以光标为中心 | 双击放大/还原 |
| 未放大时横向滑动切换 | ✅ | ✅ 横向双指滑动上一/下一张 | 阈值 60，方向正确 |
| 方向键 / 翻页按钮 | ✅ | ✅ ←/→ + chrome 按钮 | 与滑动并存 |
| 滚轮缩放 | 部分 | ⛔（用捏合/双击） | 非必须 |

**照片网格**

| 手势 | 系统照片 | 本应用 | 验收 |
| --- | --- | --- | --- |
| 捏合改变缩放档位 | ✅ | ✅ **每手势仅一档**（1/3/5/7/9/15/27） | 不再一次跳多档 |
| 滚动浏览 | ✅ | ✅ | 流畅、预热不卡 |
| 单击打开 | ✅ | ✅ | — |
| 右侧年/月快速滚动导航 | ✅（Photos 右侧 scrubber） | ✅ 新增（点击年份 / 拖拽 scrub） | 见 8.3 |
| 方向键移动选中 | ✅ | ⛔ 暂未做（NSCollectionView 默认部分支持） | 后续可补 |
| 多选（⌘/⇧ 点击、框选） | ✅ | ⛔ 当前单选 | 后续可补 |

### 8.3 年/月滚动导航验收

- 滚动时右侧导航淡入、停顿约 1.4s 淡出。
- 显示年份刻度，点击跳到该年第一张。
- 上下拖拽：玻璃胶囊跟随显示当前「年月」，松手/拖动时跳到对应月份起始。
- **重点排查**：右侧约 48px 交互带是否误吞网格点击/滚动（必要时缩窄或仅在 active 时 `allowsHitTesting`）；拖拽实时滚动是否过频（可加节流）；年月标签中英文格式随语言切换。

### 8.4 其余三项（本轮同批，已在第 9 节风格但此处补记）

- 非照片页顶部大空白：内容区 `maxHeight:.infinity` 撑满修复。
- 中文本地化：`AppLocalizer` 改读编译后 `.lproj`；语言项用母语名（简体中文/English）。
- 液态玻璃：侧栏/导航坞/Find/工具条改 `.glassEffect`。
- 验收：切简体中文后标签/设置变中文；三个非照片分区顶部无空白；侧栏与导航坞有玻璃质感。

---

## 9. P2-A / P2-B：元数据 + 语义 + 多任务（**未提交，在工作区**）

> 本轮在沙箱内**未编译**。向量索引依赖 Apple `VNGenerateImageFeaturePrintRequest`（系统自带，无需模型文件）；文字语义（MobileCLIP/MLX/JINA V5）已留策略接口但未接入。

### 9.1 新增文件

- `Domain/Embedding.swift`：`EmbeddingProvider` 策略接口（模型/维度/模态无关）+ `AppleFeaturePrintProvider` + 预留 MobileCLIP/MLX/**JINA V5** 描述符 + `EmbeddingProviderRegistry` + `VectorMath`（cosine/编解码）。
- `Domain/PhotoAnalysisTask.swift`：`AnalysisKind`(5 类) / `TaskProgress` / `PhotoAnalysisProcessor` 协议 / `AnalysisTarget` / `AssetImageLoader`（跨源取图）。
- `Infrastructure/Database/AnalysisDataRepository.swift`：嵌入（空间 ensure/upsert/缺失/**暴力 KNN**/30 天清理）、标签（upsert+搜索文档+topTags）、地名（upsert/读）、类型(RAW)、分析状态（幂等标记）。
- `Infrastructure/Analysis/AnalysisProcessors.swift`：标签(`VNClassifyImageRequest`)/地名(`CLGeocoder`+EXIF GPS)/类型(`UTType.rawImage`)/向量索引 4 处理器。
- `App/TaskCenter.swift`：多任务调度（最多并发 2、开始/暂停/停止/进度、开机自启、向量空间清理）。美学评分复用既有 `AnalysisViewModel`。
- `Presentation/Shell/MultiTaskStatusView.swift`：右上角多任务状态弹层。
- migration 6：`embedding_spaces` + `photo_embeddings`(按 `space_id` 索引、变长 blob) + `photo_analysis_state` + `photo_assets.is_raw` + 默认向量模型设置。

### 9.2 向量库设计（按你的要求）

- 不同模型向量长度不同 → 单表 `photo_embeddings(photo_id, space_id, dimension, vector blob)`，对 `space_id` 建索引；KNN 始终 `where space_id = ?`，**只用指定模型的向量**。
- 切换模型：新空间无向量 → 向量索引任务生成；旧空间保留，**超 30 天未用自动清理**（`cleanupStaleSpaces`，开机时跑）。
- 接 JINA V5：实现一个 `EmbeddingProvider`（设 `dimension`/`modality`/`supportsTextQuery`）并在 `EmbeddingProviderRegistry` 登记即可，DB 无需改。

### 9.3 编译风险热点（先看）

- **Swift 6 并发**：`TaskCenter` 的 run loop（@MainActor）+ `Task.detached { processor.process }`；`AssetImageLoader`/processors 的 Sendable；`AppleFeaturePrintProvider` 内 `Task.detached` 跑 Vision。
- **Vision 解码**：`VNFeaturePrintObservation.elementType/.data/.elementCount` → `[Float]`（`Embedding.swift` decode）。
- **`Data(buffer:)` / `withUnsafeBytes.bindMemory(to: Float.self)`** 的向量编解码（`VectorMath`）。
- **CLGeocoder**：`reverseGeocodeLocation(_:preferredLocale:completionHandler:)` + 限流（已加 300ms 节流、小批量）。
- **`UTType.rawImage` / `PHAssetResource`**（类型 RAW 判定，需 `import UniformTypeIdentifiers`/`Photos`）。
- **GRDB**：blob 读写、`StatementArguments(ids)`、新表多语句 migration 在**旧库升级**路径。

### 9.4 功能验收

- 右上角可同时查看 5 类任务进度，并分别开始/暂停/停止；开机自动跑向量索引/标签/地名/类型；**同时最多 2 个并发**，其余排队。
- 自动标签跑完后：Find 浮层「Tags」出现真实标签 chip，可勾选筛选（`tagIDs` 已接 SQL）。
- 地名跑完后：查看器详情显示「地点」；类型显示 RAW 标记。
- 向量索引跑完后：查看器「查找相似照片」可用 → 照片墙变为相似结果（顶部「相似照片结果·清除」横幅退出）。
- 设置页「智能分析」可切换向量模型（当前仅 Apple 可选，其余标注待接入）。

### 9.5 已知取舍（非缺陷）

- **类型任务**较轻（主要补 RAW 标记）；视频的向量/标签用首帧。
- 美学评分仍走既有引擎，TaskCenter 仅统一展示与并发计数（菜单里的「评分」入口不变）。

---

## 10. 自定义 MLX 模型 + sqlite-vec + 完成度/语义（**未提交，工作区**）

> 本轮含**两个必须在 Mac 上补的外部依赖**，沙箱无法引入/验证：
> 1. **sqlite-vec**（C 扩展，向量 KNN，支撑百万级）；2. **mlx-swift + JINA V5 模型**（文字语义推理）。
> 两者都做了「自动探测 + 优雅降级」：没有时向量检索退回暴力 KNN、文字语义提示「运行时未接入」。

### 10.1 新增 / 改动

- `Domain/MLXEmbeddingProvider.swift`：`MLXEmbeddingRuntime` 运行时 seam（`MLXRuntimeFactory.make` 注入点）、`CustomMLXEmbeddingProvider`、`CustomVectorModelConfig`（目录书签持久化）。
- `Infrastructure/Database/VectorIndex.swift`：`VectorIndexEnvironment.probe`（探测 sqlite-vec）、`SQLiteVecIndex`（每模型一张 `vec0` 定维表 + KNN）。
- `Embedding.swift`：注册表支持自定义模型（线程安全 holder + 解析后的目录 URL）。
- `AnalysisDataRepository`：`upsertEmbedding` 同时写 sqlite-vec；`nearest` 有扩展走 sqlite-vec、否则暴力；新增 `totalPhotoCount`/`countPendingScores`。
- `AppState`：`configureCustomVectorModel(directoryURL:)`（导入/切换/起索引）、开机解析持久化的自定义模型、`VectorIndexEnvironment.probe`、读模型 `config.json` 维度。
- `TaskCenter`：`completion`/`completionPercent`/`refreshCompletion`（完成度%）；取批改后台执行（不卡 UI）。
- `SettingsPageView`：「向量模型」选择 + 「导入本地模型目录…」(NSOpenPanel) + 「分析任务」列表（各任务 % + 启动/暂停/停止）。
- `PhotoLibraryViewModel.semanticSearch`（文字→图片）；`FindOverlayView` 在模型支持文字时显示「语义搜索」。

### 10.2 Mac 上必须补的依赖

- **sqlite-vec**：通过 SwiftPM 引入并注册（`sqlite3_vec_init` / `sqlite3_auto_extension`），使 `vec0` 虚拟表可用；`VectorIndexEnvironment.probe` 会自动检测。验证 `SQLiteVecIndex` 的 DDL/KNN 语法（`embedding match ? order by distance limit ?`、float[N] blob 绑定）与你引入的 sqlite-vec 版本一致。
- **mlx-swift + JINA V5**：实现 `MLXEmbeddingRuntime`（加载 `jina-embeddings-v5-omni-small-mlx` 目录、出 1024 维 image/text 向量），并把 `MLXRuntimeFactory.make` 指向它。未接入时自定义模型可被选择但索引会失败（提示运行时未接入）。

### 10.3 编译风险热点

- Swift 6 并发：`nonisolated(unsafe) static var` + `NSLock`（注册表、`VectorIndexEnvironment`、`MLXRuntimeFactory`）；`Task.detached` 取批/处理。
- GRDB：`vec0` 动态建表、blob 绑定、`StatementArguments`；migration 6 旧库升级。
- 安全作用域书签：自定义模型目录与本地照片源同机制（start/stop 配对、失效重授权）。

### 10.4 功能验收

- 设置「向量模型」可选 Apple / 已导入的自定义模型；导入目录后自动切换并开始索引。
- 设置「分析任务」显示 5 类任务**整数完成度%** 与进度条，可分别启动/暂停/停止；右上角控件同源。
- 切模型后：新空间重新索引；旧空间 30 天未用清理；同时最多 2 个任务并发、UI 不卡。
- 相似照片（Apple 向量，立即可用）；文字语义搜索（接 JINA+MLX 后可用）。

## 11. 九项修复/优化（**未提交，工作区**）

1. 向量数据清理：`AnalysisDataRepository.clearSpace`（删 photo_embeddings + drop vec0 表 + 删空间）；`cleanupStaleSpaces` 同步 drop vec 表；切换/删除/重建模型时清理。设置页加「重建当前模型向量」「删除自定义模型」。
2. 模型目录校验：`NSOpenPanel` 选目录后校验 `config.json` + 权重文件（safetensors/npz/bin…），缺失则失败并在设置页红字提示缺哪些；不硬编码路径。
3. i18n：`TaskCenter.localeIdentifier` 默认跟随系统、随应用语言更新 → 地名按当前语言存储。`en_US_POSIX`（EXIF 日期解析）属固定格式必需，保留。标签存 Vision 中性标识符（非中文），其显示本地化为后续。
4. 新功能 UI 已用原生 `Form/Section/Picker/ProgressView/Label/SF Symbols/NSOpenPanel` + `.glassEffect`，无需重做。
5. 任务停不掉/计数超额：loop 加「同批无新 id 即收尾」+ completed 取唯一并 cap 到 total；失败项不再无限重处理。
6. 废片篓：批量「全部恢复」+「清空（永久删除）」（系统走 PhotoKit 批量删除、本地进废纸篓）；带二次确认。
7. 左侧抽屉：Tab 键切换（照片页、无浮层/文本框聚焦时）；移除小箭头，改顶部 `sidebar.leading` 原生按钮；折叠时不拦截点击。
8. 查看器快捷键：恢复 F=收藏 / I=信息 / D=删除（仍弹确认）/ T=废片篓（单键）。
9. 网格：间距 16→2、缩略图去圆角；列数≥15 隐藏分数角标，BadgeSegment 呈隐藏态、仅切维度不开启展示。

验收重点：任务能停且完成度不超 100%；废片篓批量恢复/清空；Tab 与顶栏按钮切抽屉；F/I/D/T 生效；高密度无角标；导入无效目录有明确报错；切语言后新地名为对应语言。

### 10.5 本轮暂缓（需单独一轮）

- **全代码库注释精简 / 命名自解释**：新增 P2 代码已从简；老文件的全量注释清扫建议**在 Mac 编译修订后**统一做一轮（否则与编译修复互相冲突、白改）。
- **更深的架构重构**：当前分层已较清晰（处理器统一协议、向量空间隔离、provider 策略），建议待整体编译跑通、有真实性能数据后再针对热点重构。

---

## 11. Swift 6 + GRDB 7 编译实测踩坑（2026-06-11，Mac 已修）

本轮在 Mac 上跑 `swift build` 真实验证后新增的两条经验，前置条件：Swift 6.3.2 + `swift-tools-version: 6.0` + GRDB 7.11+（`@_disfavoredOverload` 标注的 sync `write<T>`）。

### 11.1 `try?` + 多语句 throwing 闭包 → 闭包返回类型推断失败

**症状**：
```
error: missing return in closure expected to return '(any (~Copyable & ~Escapable).Type)?'
```

**触发**：
```swift
let ok = (try? databaseQueue.write { db in
    try db.execute(sql: "...")
    try db.execute(sql: "...")
}) != nil
```

**根因**：Swift 6.3 编译器在 `try?` 包装多层 throwing 闭包时，闭包的 `T` 推不出 `Void`，回退到 Swift 6 存在类型元类型。

**修复**（二选一）：
```swift
// 方案 A：do-catch 显式分支（推荐，可读性最好）
let ok: Bool
do {
    try databaseQueue.write { db in
        try db.execute(sql: "...")
        try db.execute(sql: "...")
    }
    ok = true
} catch { ok = false }

// 方案 B：显式标注闭包返回类型
let ok = (try? databaseQueue.write { db -> Void in
    try db.execute(sql: "...")
    try db.execute(sql: "...")
}) != nil
```

### 11.2 `Task.detached` 闭包中隐式 self 捕获

**症状**：
```
error: reference to property 'X' in closure requires explicit use of 'self' to make capture semantics explicit
```

**触发**（`App/TaskCenter.swift:188`）：
```swift
let batch = await Task.detached(priority: .utility) {
    (try? processor.nextBatch(limit: batchSize)) ?? []   // ← batchSize 隐式 self 捕获
}.value
```

**根因**：`Task.detached` 的闭包是 `@Sendable`，Swift 6 严格并发不允许在 `@Sendable` 闭包里隐式 `self.X`，必须用捕获列表显式表达。

**修复**：用 `[batchSize]` 把值在闭包创建时快照下来（不持有 self）：
```swift
let batch = await Task.detached(priority: .utility) { [batchSize] in
    (try? processor.nextBatch(limit: batchSize)) ?? []
}.value
```

> 注：`[batchSize]` 是 `[batchSize = self.batchSize]` 的简写。如果闭包体里还要访问 self 其它成员，要写 `[batchSize, self]` 或 `[self = self]`。

### 11.3 同类风险（**未修，待评估**）

`App/TaskCenter.swift` 中 `processorProgress[kind]?.status = ...` 这类写操作是 class 字段跨 Task 共享，**Swift 6 严格并发下大概率有 data race 警告**。当前 build 还没报是因为还没开更严格的 `-strict-concurrency=complete`。建议在 `Package.swift` 加 `swiftSettings: [.enableUpcomingFeature("StrictConcurrency")]`，或把 `TaskCenter` 标 `@MainActor`，再跑一遍看警告。

---

## 12. 注释精简 + 文档同步（2026-06-12，本次提交）

§10.5 曾把「全代码库注释精简」推迟到 Mac 编译稳定后再做；本轮已执行（仅注释层面，不改任何代码逻辑 / 方法签名）：

- 扫描 `MantaPhotosMac/**/*.swift` 全部源码：**无注释掉的废弃代码、无 TODO/FIXME、无显而易见的赘述行内注释**——既有注释绝大多数为「解释 why」的设计/并发/外部依赖说明与公共 API 文档注释，按规则**全部保留**。
- 仅对**重复 / 过度冗长**的两处文档块做精简：`Design/DesignSystem.swift` 的 Liquid Glass 令牌说明收敛为 3 行（保留 material-first 与「随外观自适应」要点）；`PhotoViewerView.swift` 中 `stillImage` 的手势说明与类型级 `ZoomableImageView` 文档重复，改为指向后者。
- 每个改动文件做括号 / 引号配平静态校验（本环境无 Swift 工具链，无法编译，仅静态检查）。
- 文档同步：`SPEC.md` 第九章新增「9.9bis 本轮修复与最终态补充」、更新 9.4 查看器快捷键（F/I/D/T）与 P2-A 反向地理编码（`MKReverseGeocodingRequest` 取代 `CLGeocoder`）；本清单新增本节。

> 仍未做：Mac 上 `swift build` / `xcodebuild` 真机验证（沙箱无 Swift 工具链），见各节「编译风险热点 / 功能验收」。
